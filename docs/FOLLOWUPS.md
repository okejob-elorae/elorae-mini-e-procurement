# Follow-ups / tech debt (checklist)

The canonical, actionable list of known debt and deferred work, grouped by area. Read the relevant section before starting work on that area — an item here is usually the reason the code looks the way it does. Split out of `CLAUDE.md` on 2026-08-25; the text is unchanged, and the rule stated just below still applies: new follow-ups go in this file, not in a PR body.

Canonical home for follow-ups + known debt. **New follow-ups go in this file (`docs/FOLLOWUPS.md`)** as `- [ ]` — not buried in PR bodies (they die on merge) or per-slice decomposition prose. When an item ships, flip it to `- [x]` and append the PR #. Feature names only (no EPIC labels — shared artifact). Cross-cutting code landmines also get a `memory/` note; this list is the actionable index.

Roadmap slices (not debt) live in `docs/EPIC-STATUS.md` + the GitHub board, NOT here. **Every item below was verified STILL-OPEN against current code on 2026-07-25** (full merged-PR + board sweep, then per-item code check). *Dropped as already-fixed — do not re-add:* P2002 index-name check (`stock-writer.ts:86` robust regex, #73), canvassing van-load forbidden-vs-not-found (no conflation, #148), oversold-only view (now server-side, #133), `docker builder prune -af`/`.next` cache (uses GHA layer cache, not aggressive prune, #99), `@types/node-cron` ("dead" but node-cron is actively used), fulfillment e2e (prod-live since #8).

### Finance — Journal & CoA
- [ ] **⛔ GL CUTOVER IS BLOCKED ON A BUSINESS ANSWER, NOT CODE — the purchase side of the business was never entered.** Measured on PROD 2026-08-08: **0 purchase orders, 0 GRNs**, and **0 of 9,389** eligible sales orders carry any COGS (15,852 lines: 9,116 NULL, 6,736 explicitly `0`, none positive). `InventoryValue` holds 51,944 units at `SUM(totalValue) = 0.00`. The zeros are RECORDED, not missing — `consumeOrder` ran 9,213 times and correctly stamped `cogs = 0` because `avgCost` was 0 every time. The fulfilment pipeline works; it is faithfully recording that the goods cost nothing. **Both previously-proposed remedies are dead:** a COGS backfill has nothing to backfill FROM (GRNs establish moving-average cost and there are none), and relaxing the sweep predicate would post revenue with no cost forever — a permanent 100% gross margin, turning a visible stall into an invisible falsehood. **Do not seed the CoA, map roles, or arm the cutover.** The open question is where purchases are recorded today and whether that data can enter the system; written up for the client in `docs/superpowers/specs/2026-08-08-gl-blocker-client-note.md`. Everything else (ingestion, fulfilment, stock, field sales, canvassing, settlement reconciliation) is unaffected.
- [ ] Sweep-stall mechanic, still true and worth keeping even after the above is resolved: `postPendingSalesJournals` selects on `NOT (EXISTS revenue_journal AND EXISTS cogs_journal)` and takes the oldest 50, so an order whose Σ `SalesOrderItem.cogs` is permanently 0 can never satisfy the COGS half and keeps its slot in that window forever — tick 1 posts revenue for the oldest 50, every later tick re-selects the same 50, increments nothing (`pending` counts only non-`NOTHING_TO_POST` failures) and logs nothing, so order 51 onward is never journaled with no error anywhere. `set-gl-cutover-date.sql` pre-flight query 5 measures it per environment and the file refuses to be run until it reads 0 (PR #222).
- [ ] Settlement journals are NOT gated by the GL cutover, unlike sales and returns: `finance/settlement/journal.ts` credits AR unconditionally, so the first post-cutover settlement — which covers pre-cutover shipments — credits AR that was never debited in this ledger and drives it negative. The sales and return doors are now shut; this one is open (PR #222).
- [ ] `apps/api/src/jubelio/handlers/salesorder.handler.ts` stamps `shippedAt = completed_date ?? last_modified ?? new Date()` with `reportsShipped` keyed on `wms_status` alone, so re-ingesting a historical order after go-live (the settlement resync path does exactly this) with no `completed_date` stamps `shippedAt = now`, clearing the cutover floor and journaling a pre-cutover sale into an open period (PR #222).
- [ ] `finance.glCutoverDate` has no Settings card, no notification and no UI of any kind — it is set only by hand-run SQL, and the sole signal that the sweep is inert is a 5-minute `console.warn` into an unretained container log (PR #222).
- [x] Account-mapping UI: unmap/clear control (X button per mapped role) — PR #162.
- [ ] Prod's chart of accounts was NEVER BUILT — it is the bare 10-account SAK-EMKM skeleton with no postable detail leaves, zero `JournalAccountMapping` rows and zero journals (verified 2026-08-08). The `13 Hutang` mistyping this item used to describe was DEV, not prod. Nothing to audit until a chart exists, which is gated on the GL blocker above. When it is built: every posting role must map to a LEAF — `postJournal` rejects an account with children and only checks at write time, so the mapping page shows a broken mapping as healthy.
- [ ] `Kas` (1101) has no posting role — LIVE and URGENT: van load/sale/reconcile journals shipped in PR #218 (merged 2026-08-05) and are on master, so every van sale flags `JOURNAL_PENDING` until `CASH` is mapped to it and `BANK` to 1102. **Map `CASH` and `INVENTORY_VAN` BEFORE the next van load** — see the in-flight-van hazard below (a van already holding stock at that deploy never had its load debited to `INVENTORY_VAN`).
- [x] `fix-hutang-account-type.sql` — run on dev 2026-08-06 (`affectedRows = 1`; `13 Hutang` ASET → `2101` LIABILITAS under `21`, all 4 journal lines intact, AP mapping followed by id). **It is a DEV-ONLY artifact and must never be run on prod**, where there is no `13` account and no AP mapping — it would no-op and tell you nothing.
- [ ] In-flight-van hazard: any van already holding stock when the van cash journals deploy was loaded BEFORE the load journal existed, so its value was never debited to `INVENTORY_VAN`. Its next sale credits `INVENTORY_VAN` against an account never debited, and its reconcile debits `INVENTORY` for a return that was never credited — driving `INVENTORY_VAN` negative and overstating `Persediaan`. Check `VanStock WHERE qty > 0` before mapping the `CASH`/`INVENTORY_VAN` roles; remedy is either (a) reconcile every van to empty first, or (b) post one manual opening journal (DR `INVENTORY_VAN` / CR `INVENTORY`) for the in-flight van book value.
- [ ] `VanStock.avgCost` is `Decimal(15,2)`, so a second load into a non-empty van re-blends at 2dp and leaves cent-scale residue in `INVENTORY_VAN` (bounded ≈ qty × 0.005 per blend; sub-rupiah in IDR).
- [ ] Van loads move `FINISHED_GOOD` stock against the `INVENTORY` role, not `INVENTORY_FG` — consistent with GRN, opname and marketplace COGS, but `INVENTORY_FG` remains debited by FG receipt and credited by nothing, so "GL Persediaan agrees with the stock ledger" only holds if both roles map to the same account or the one-way FG account is accepted.
- [ ] The konsi retur's decrement of `StoreStock` writes **no `StockAdjustment`**, while the transfer out of main does — so goods leaving a consignment store have no audit row on the store's own ledger. The Inventory Extensions criterion "all movements create audit trail entries" is therefore only half met, and is annotated PARTIAL on the board rather than ticked (feat/konsi-virtual-warehouse).
- [ ] The konsi stock move (main → `StoreStock` at approve) writes NO GL journal at all, while van load journals the analogous main-leaves move — so once the cutover is armed, "GL Persediaan agrees with the stock ledger" diverges by whatever volume moves through konsi transfers (feat/konsi-virtual-warehouse).
- [ ] `postJournal`'s findUnique-then-create means two concurrent posts for the same source can file a `JOURNAL_PENDING` with `reason: "ERROR"` on a P2002 even though the journal did post — a false alarm, no duplicate and no double-count.
- [ ] Van "Post journal" retry visibility (`lib/canvassing/journal-pending.ts`) reads every `JOURNAL_PENDING` row and matches `metadata.docId`/`kind` in JS, because JSON-path filtering is unreliable on this adapter. Uncapped by choice — a `take` window would hide a genuinely failed older post — and cheap today (6 such rows on dev). Revisit if `AdminNotification` grows, since nothing prunes it.
- [ ] Van journal retry is gated on a `JOURNAL_PENDING` notification existing (`hasPostableJournal`), but `postVanJournalSafely`'s own `notify` write is itself best-effort — if the journal post fails AND the notification write also fails (or the process dies after the stock transaction commits but before `notify` runs), the document has no journal, no notification, and no retry button: permanently unpostable except by hand. We can't add a durable attempt marker without a migration, so the real fix is a van equivalent of `lib/finance/sales/sweep.ts`'s `postPendingSalesJournals` — a sweep over van load/sale/reconcile documents that have no journal, independent of whether a notification was ever written. Must be scoped to specific document ids or a date window, per this file's own rule that a test-bed spec must never invoke a DB-wide sweep (see "What NOT to do").
- [ ] Seed CoA detail postable leaves — Persediaan, Piutang, Selisih Persediaan, Marketplace Fee (Bank `1102` already seeded) so posting-role mappings are wireable out-of-box. Seed is a 10-account SAK-EMKM skeleton (`coa-sak-emkm.json`).
- [ ] `postJournal` integrity-check query — surface any unbalanced or dangling journals.
- [ ] Settlement journal `UNBALANCED` when the excel summary itself doesn't satisfy `Dilepas + Pengeluaran = Pendapatan`. The `MARKETPLACE_FEE_OTHER` residual absorbs fee-level gaps but is computed from `totalPengeluaran`, so it cannot absorb a summary-level mismatch (`settlement/journal.ts`) (PR #157, #17).
- [ ] Settlement `SP-`+orderNo match coverage is low for pre-2026-06-14 settlements — revisit if old-period reconciliation matters.
- [ ] Shopee fee itemization is coarse: `SettlementLine` persists 4 of ~10 charge columns on the Income sheet (captured: Komisi AMS, Administrasi, Layanan, Proses Pesanan; not itemized: Premi, Biaya Transaksi, Biaya Kampanye, Bea Masuk/PPN/PPh, Biaya Program Hemat Biaya Kirim, Biaya Isi Saldo Otomatis). Not a correctness gap — the journal totals from Summary's `Total Pengeluaran` with `MARKETPLACE_FEE_OTHER` absorbing the rest, and per-line profit uses `Total Penghasilan`, already net of every fee — so this only limits Laba Rugi's fee breakdown granularity. Verified against `reference/finance/Income.sudah dilepas.id.20260601_20260630.xlsx` (43 columns) on 2026-08-05. Adding columns means a migration + parser + journal-role work.
- [ ] `Biaya Layanan` is itemized per order on Shopee's Seller Fee sheet (Gratis Ongkir XTRA kategori F/G, Promo XTRA, Shopee Live XTRA) but only stored as raw JSON in `Settlement.sellerFeesRaw` — never parsed into per-line columns. Same granularity theme as the item above.
- [ ] Field-sales promo: order-level discount not pro-rated across `SalesHistory` lines — line totals sum > order total (`field-sales/writer.ts:194`), per D7 (PR #111).
- [x] Auto-journal supplier payments (DR Hutang / CR Bank) — the PO paid toggle now posts DR AP / CR BANK for the payable its receipts actually booked to the GL, and unmarking posts the mirror (PR #220).
- [ ] A PO marked paid with nothing booked raises `JOURNAL_PENDING` and posts nothing — the payment direction notifies on `NOTHING_TO_POST`, while the unmark direction stays silent because nothing to reverse is a genuine no-op. After the GRN-journal precondition below, the only causes left for that code are a genuine advance payment (no receipts), no receipt that could ever book a payable (each one sub-cent or owner-declined), or a payable already cleared by reversals; a receipt still owed with no journal returns `GRN_JOURNALS_INCOMPLETE` and one still awaiting the owner's decision returns `GRN_APPROVAL_PENDING` instead. Correct today, but advances have no representation in the GL at all until a real payment model exists.
- [ ] Supplier payment now REFUSES (`GRN_JOURNALS_INCOMPLETE`, raises `JOURNAL_PENDING`, posts nothing) while any receipt of the PO that is STILL OWED is missing its GRN journal — one of several or every last one — correct, because paying only the journaled subset under-pays and the rest reappears in payables the moment that GRN journal is retried. Owner-declined receipts are exempt (same as sub-cent ones: nothing was ever booked for them), so a declined receipt whose journal never posted no longer blocks its siblings and no longer gets told to post a journal for goods inventory already reversed — that PO now reports `NOTHING_TO_POST` when the declined receipt is its only one. The consequence is a second downstream effect for a stuck GRN `JOURNAL_PENDING`: it now blocks that PO's payment journal too, not just the receipt's own posting. Remedy is per-PO (post the missing GRN journal, then unmark + re-mark payment); a GRN journal sweep would clear both at once.
- [ ] Supplier payment also REFUSES (`GRN_REVERSAL_MISSING`, raises `JOURNAL_PENDING`, posts nothing) while an owner-declined receipt still carries its GRN journal with no `GRN_REVERSAL` — the declined payable is otherwise netted into the payment and over-pays payables and bank by that amount. Same downstream shape as the incomplete-journal refusal above, but a separate code because the remedy is the declined GRN's own reversal retry, not the receipt-journal one. Remedy: post the reversal from the GRN row, then unmark + re-mark payment.
- [ ] Supplier payment also REFUSES (`GRN_APPROVAL_PENDING`, raises `JOURNAL_PENDING`, posts nothing) while any receipt of the PO is an over-receive still awaiting the owner's approve-or-decline. An over-receive credits payables in full at receive time and only then waits, so paying before the decision and declining after leaves the bank cash-out standing while the decline debits payables a second time — payables negative, supplier overpaid on the books, and nothing adjusts the payment. Condition mirrors `declineGRNByOwner`'s own guard (`requiresOwnerApproval` set, neither `ownerApprovedAt` nor `ownerDeclinedAt` stamped), checked FIRST of the three preconditions because the decision is a prerequisite of the other two remedies. Remedy: owner approves or declines the pending receipt, then unmark + re-mark payment. Consequence: an over-receive left undecided now blocks its PO's payment journal, not just its own approval state.
- [ ] The `paidAt` toggle and its journal now run in ONE serializable transaction (`runSerializable`, retried on deadlock), so a mark and a concurrent unmark can no longer interleave — the old compare-and-swap only ordered the two `paidAt` writes, leaving a window where an unmark reversed nothing (silent `NOTHING_TO_POST`) and the mark's journal committed after it, ending unpaid with DR payables / CR bank standing. Residual by design: a journal failure still leaves the toggle committed (the post's error is caught inside the transaction and carried out), so the `JOURNAL_PENDING` notification — written after the transaction, never inside it — remains the only recovery signal, and the recovery is still unmark + re-mark. Both post-commit writes are hardened around that: the notification is written BEFORE the `POStatusHistory` row (a bookkeeping write must not be able to skip the durable flag) and the history insert is itself try/caught-and-logged (its failure must not reject the action and replace the operator's warning toast with a generic error).
- [ ] Supplier payment also REFUSES (`PAYMENT_SUPERSEDED`, raises `JOURNAL_PENDING`, posts nothing new) when a payment journal is already standing at the current generation for a DIFFERENT amount than the payable now reads. The generation counts reversals, so a reversal that failed to post freezes it while `paidAt` keeps toggling: mark (posts `poId#1`), unmark (reversal fails, `#1` still standing), journal another receipt, re-mark → `postJournal` matches `#1` and would have reported `created: true`-shaped success while the ledger cleared only the old, smaller amount. The refusal compares the standing journal's own posted total against the fresh payable in cents. Unlike every other code, payables and bank are NOT untouched here — they hold the earlier payment — and the message says so. Remedy: confirm the reversal actually posted, then re-mark. Residual: if the reversal keeps failing (e.g. an unmapped role), the PO stays stuck at the stale amount until that is fixed — by design, since the alternative is a second payment journal on the same generation. **Note the re-mark leg of that sequence is now blocked before it reaches this code** — `setPOPaidAt`'s `STANDING_PAYMENT_JOURNAL` guard refuses the mark while the reversal is still missing, and the reversal is posted from its own control on the PO detail page rather than by toggling (PR #220).
- [ ] BACKFILL GAP: every PO already marked paid before the supplier payment journal deploys has no `SUPPLIER_PAYMENT` journal and nothing creates one, so prod payables overstate by the total those receipts booked and supplier cash-out is missing for the whole pre-feature history. The only backfill today is per-PO unmark + re-mark (the unmark returns `NOTHING_TO_POST` and posts nothing, the re-mark posts at generation 1). The durable fix is a scoped sweep over `PurchaseOrder WHERE paidAt IS NOT NULL` with no payment journal — scoped to explicit ids or a date window, per this file's rule that a test-bed spec must never invoke a DB-wide sweep.
- [ ] `postGrnReversalJournal` dates its reversal `new Date()` instead of the GRN/journal it reverses (`inventory/grn-journal.ts`), so declining an over-receive GRN in a later period misstates both periods — the same defect the supplier payment reversal now avoids by dating to the payment journal's own date. Out of scope on the supplier-payment branch.
- [ ] Supplier payment assumes bank transfer (`BANK`); cash payments to suppliers cannot be represented without the payment model.
- [ ] `paidAt` remains a bare toggle: no amount, no method, no partial payments, no paying several POs in one transfer. A real `SupplierPayment` table (amount, date, method, account, allocation) is the eventual fix and would replace the derive-from-ledger approach. **The 2026-08-06 state-matrix audit's verdict is to schedule this rather than add a seventh refusal code** — the generation counter, `PAYMENT_SUPERSEDED`, the standing-payment detector and its recovery action are each cleaning up after the previous one's residue, and each new layer is where the next defect has lived. Arithmetic is sound (32 reachable cells walked, no state where the GL ends wrong with no exit); the state model is what keeps leaking.
- [ ] DECISION NEEDED — should a document-state refusal ABORT the paid toggle instead of committing `paidAt`? Now scoped to THREE codes, not four: the `PAYMENT_SUPERSEDED` case was settled the other way late in PR #220, by a separate pre-CAS guard in `setPOPaidAt` that returns its own `refusal: "STANDING_PAYMENT_JOURNAL"` whenever the PO reads unpaid with a payment journal still standing. That guard preempts the writer's `PAYMENT_SUPERSEDED` rather than being it — the writer code is unchanged and still refuses post-CAS if ever reached directly. Committing that state through destroys its own recovery banner, and both UIs already withheld the mark, so enforcing it server-side was consistency rather than a new policy. The open three are `GRN_APPROVAL_PENDING`, `GRN_JOURNALS_INCOMPLETE`, `GRN_REVERSAL_MISSING`, which still leave `paidAt` set — as do the infrastructure codes (`UNMAPPED_ROLE`, `UNBALANCED`, `ERROR`), correctly, since a Finance misconfiguration must not block ops. For the three the refusal means "this PO is not payable yet, fix that document first", and committing `paidAt` manufactures the ledger/UI divergence the feature fights. The post runs inside the toggle's transaction, so aborting is a small change per code. Blocked on an owner call: whether ops may record cash leaving the bank while a receipt is still unapproved. **Unlike the superseded case, these three refuse in states where the UI still OFFERS the button — aborting removes operator capability, which is why it stayed a product decision** (PR #220).
- [ ] A PO left marked paid with NO payment journal is invisible after the page reloads — the mirror state (unpaid + standing journal) got a detector, a banner, and a withheld mark on both surfaces; this one has only the transient toast plus an `AdminNotification` nothing renders. A naive "paid without a payment journal" banner would false-positive permanently on the legitimate cases (advance payment, all-sub-cent, all-declined) with no dismiss path. So the fix is a payment-journal health panel on the PO detail (re-run `poBookedPayable`, show the code + the toast's own remedy sentence + a retry), not a one-line detector — or nothing, if the abort decision above is taken, since that removes most of what the panel would flag. The superseded half of this item is closed: the mark that used to create it is now refused (PR #220).
- [x] `/backoffice/supplier-payments` standing-payment signal — the register now disables Mark paid on exactly the POs the detail page does, off the same condition, with the reason rendered inline; `listPOs` gained an opt-in `withStandingPaymentJournal` extra backed by `poIdsWithStandingPaymentJournalWhileUnpaid` (fixed 3 queries per page, not 3 per row) (PR #220).
- [ ] Recovery still lives ONLY on the PO detail page — the register names the standing-payment state but carries no "Post reversal journal" control, so clearing it always means opening the PO. Narrowed, not closed, by the item above (PR #220).
- [ ] The list-row standing-payment flag is OPTIONAL on the row type and opt-in per caller, so a future third paid-toggle surface that forgets `withStandingPaymentJournal` silently gates nothing — fail-open. Optional on purpose (a caller that never checked must not be handed `false`, which reads as "safe to mark"), but only two surfaces exist today to keep it honest (PR #220).
- [ ] `poIdsWithStandingPaymentJournalWhileUnpaid`'s reversal read is one query with an `OR` term per unpaid id: round trips are constant, predicate width is not. Fine for a paginated caller; an unpaginated one asking for the flag is the thing to review (PR #220).
- [ ] `PAYMENT_SUPERSEDED` is now close to unreachable from the UI, since `setPOPaidAt`'s `STANDING_PAYMENT_JOURNAL` guard refuses the mark before the CAS that would reach it. Kept as the writer-level backstop (`postSupplierPaymentJournal` is independently callable), but it is exercised almost entirely by its spec now — don't mistake it for dead code (PR #220).
- [ ] The PO detail page hides both paid-toggle buttons for `DRAFT`/`CANCELLED` while the supplier-payments register shows them for every row, so a cancelled PO's paid mark is editable from one surface and not the other. Never a GL question — a payable needs a GRN and cancellation is blocked once any receipt exists — but the two surfaces should agree (PR #220).
- [ ] `getGrnJournalState.hasPostableReceiptJournal` is `hasValue && receipt == null` with no declined check, so the inventory UI offers "Post journal" on an owner-DECLINED GRN; taking the offer books a payable for goods inventory already reversed and pushes that PO into `GRN_REVERSAL_MISSING` (recoverable from the declined GRN's own reversal button). Pre-existing GRN-side wart, surfaced by the supplier payment audit, untouched by that branch.
- [ ] Van journal `JOURNAL_PENDING` dedup (`lib/canvassing/post-van-journal-safely.ts` `alreadyFlagged`) matches only `(docId, kind)`, so a van document that fails one way, gets fixed, and then fails a DIFFERENT way writes no new row — the operator is left with the stale first reason. Same weakness the supplier payment dedup just fixed by adding `reason` to the match; exposure is smaller because the van flow has only three distinct reasons (`UNMAPPED_ROLE`, `UNBALANCED`, `ERROR`) and they share one remedy family (map the account, then retry), whereas supplier payment has a longer list of codes with genuinely different remedies. Fix is the same one-line predicate change, deliberately left out of the supplier payment branch. Note `hasPostableJournal` gates the van retry button on ANY matching row, so the button survives either way — this is a wrong-message bug, not a lost-retry one.
- [ ] Supplier payment `JOURNAL_PENDING` dedup matches `(docId, kind, reason)`, so at most one unread row per distinct reason per PO and direction. Two residuals: an earlier reason's row lingers unread after the failure mode changes (nothing marks it superseded, so a feed would show both — the newest is correct, the older is history), and every `ERROR` collapses to one row regardless of the `detail` message. Marking read clears the first; both wait on the `AdminNotification` feed follow-up.
- [ ] Van journal outcome is invisible at the moment of the action: `loadVanAction`/`recordVanSaleAction`/`recordVanReconcileAction` call `postVanJournalSafely` and then return `{ok:true}` regardless, so `LoadVanForm`/`ReconcileVanForm`/`VanSellShell` always `toast.success` — the operator is told the document succeeded even when nothing posted to the GL. Same shape the supplier payment toggle just fixed by returning the journal failure and branching to a warning toast (`supplierPayments.journal.err.*` + `supplierPaymentJournalErrorKey`). Exposure is smaller than supplier payment's was, because the van doc detail pages DO render a `JOURNAL_PENDING` badge + "Post journal" retry, so the signal exists — it is just not where the operator is looking. Fix mirrors the supplier payment one: return the failure code from the three actions, add `canvassing`/`vanSale` `journal.err.*` messages, warn instead of confirming. Deliberately out of scope of the supplier payment branch.
- [ ] `/backoffice/supplier-payments` and the PO detail page are otherwise 100% hardcoded English with no i18n hook; the new supplier-payment journal warning toasts are the first translated strings on them, so an `id`-locale operator now sees an Indonesian warning beside English buttons. Net gain over no warning at all, but those two pages want a proper i18n pass.
- [ ] `SUPPLIER_PAYMENT` / `SUPPLIER_PAYMENT_REVERSAL` journals key `sourceId` as `poId#generation` (generation = reversals so far + 1), not a bare PO id — any future consumer looking up a PO's payment journal must prefix-match `poId#`. Nothing consumes it today.
- [ ] Putus/konsi field-sales revenue + receivable auto-journal — all offline order revenue is still absent from the GL, so Laba Rugi understates it. Does not affect cash flow. (Van sale is now journaled — see the van cash journal follow-ups in this section.)
- [ ] SPG sales auto-journal — a KONSI-store sale now decrements `StoreStock`, but nothing SNAPSHOTS a cost at sale time: `SpgSaleLine` has no cost column, and the decrement deliberately leaves `avgCost` untouched. A journal built later would have to read `StoreStock.avgCost` as of the journal's own run, and a konsi transfer in between can re-blend that average — e.g. store holds 10 @ 12.500, the SPG sells 4 (avgCost stays 12.500), a transfer then brings in 10 @ 20.000 and blends to 17.187,50 on qty 6, so a post-cutover journal would credit 4 × 17.187,50 = 68.750 instead of the correct 50.000. So the basis exists but is not preserved; whoever builds this journal must either post at sale time (before any intervening transfer can re-blend the average) or add a cost column to `SpgSaleLine`. It stays blocked on the general GL cutover regardless (see the Finance — Journal & CoA section above). A PUTUS-store sale still moves no stock and still needs its own answer for what to credit.
- [ ] Itemize TikTok fees from `escrow_list` instead of dumping them into `MARKETPLACE_FEE_OTHER`.
- [ ] Fiscal-period close so Neraca reads real retained earnings instead of an injected unclosed-earnings line.
- [ ] Settlements journaled before the fee split keep their single lumped `MARKETPLACE_FEE` line — no reverse-and-repost migration was run.
- [ ] Settlement journal's bank and AR lines still throw an uncaught `BAD_LINE` when `totalDilepas` or `totalPendapatan` is zero (a both-zero journal line), which is realistic for a fully-refunded settlement period — same bug class as the fee-sign issue fixed in PR #215; `NON_POSTABLE_ACCOUNT` and `UNBALANCED` are handled, this case is not.
- [x] `NON_POSTABLE_ACCOUNT` settlement error string + toast case — done (PR #215); both locales name the cause (inactive or non-leaf mapped account) and the fix (Finance → Pemetaan Akun).
- [ ] Laba Rugi has no page-level empty state — it relies on per-section `emptyLabel` rows, unlike the Trial Balance sibling, so a period with no journals renders an all-zero skeleton instead of an empty state.
- [ ] Neraca's balanced/unbalanced note is on screen only — the print view and the Excel export omit it, unlike Trial Balance's tripwire note and Neraca's own opening-balance warning (both now exported). `BalanceSheet.isBalanced` is already available at both call sites; ~2 lines each.
- [ ] Statement disclosure copy is duplicated: `apps/web/lib/finance/reports/disclosures.ts` (used by the print views and Excel exports, which have no locale provider) restates the `financeReports.*` note strings in `id.json` verbatim, kept in step only by a header comment. A copy-editor touching the locale file will not know to update the other.
- [ ] Statement print views use a named `@page statement` for portrait geometry, which is Chromium-only — Firefox still prints them on the shared landscape sheet (`print.css`). Cosmetic (nothing clipped); fixing it properly needs the shared `@page` default split per print view.
- [ ] No test covers the mixed-sign settlement fee shape (positive summary `totalPengeluaran` with negative per-line fee columns) — hand-traced as safe for totals today, but a net-positive fee category would post mis-signed with the `MARKETPLACE_FEE_OTHER` residual silently absorbing the difference.
- [x] Settlement journal reported role `MARKETPLACE_FEE` when BOTH a category role and the legacy fallback were unmapped — `resolveFeeAccount` now rethrows the category role the operator actually hit, so the `UNMAPPED_ROLE` toast names the right mapping. Covered by a `journal.test.ts` case.
- [ ] Up to ten sequential account-mapping lookups run before `postJournal`'s idempotency short-circuit, so re-posting an already-posted settlement pays the full resolve cost (`settlement/journal.ts`).
- [ ] `MarketplaceFeeRole` in `finance/settlement/fee-split.ts` hand-duplicates five `POSTING_ROLES` entries — ``Extract<PostingRole, `MARKETPLACE_FEE_${string}`>`` would remove the drift risk (the compiler catches renames today, not additions).
- [ ] Laba Rugi renders every active revenue/HPP/expense account even at zero for the period, with no "show zero accounts" toggle like the Trial Balance has.
- [ ] `apps/web/lib/finance/settlement/tiktok-settlement-parser.ts`'s top-of-file comment is stale — it says the settlement journal posts `totalPengeluaran` as a single straight debit line, which stopped being true when the per-category fee split landed. Its `Math.abs()` rationale still stands.
- [ ] Cash flow print view is single-period — the previous-period comparison appears on screen and in Excel but not in the PDF, because three amount columns do not fit the portrait statement sheet. A landscape variant would fix it.
- [ ] Cash flow section classification has no bulk edit: clearing a large unclassified bucket is one account at a time. Fine for a small chart of accounts, painful for a real one.
- [x] The Klasifikasi Arus Kas page listed every balance-sheet account including non-postable parents — `listAccountSections` now returns LEAVES only, which was never cosmetic: tagging a PARENT as KAS silenced its own leaves out of the cash bucket while contributing a cash delta of zero, so the statement reported zero cash start and end under the green reconciled note. Inactive leaves are deliberately kept (not "postable leaves"), matching `getAccountBalances`, so a deactivated cash account still holding a balance stays in `cashAccountIds` and `kasAwal`; the row is dimmed on the page instead (feat/finance-cash-flow-statement).
- [ ] `getCashFlowReport` runs `getAccountBalances` twice when a comparison is shown (once per window) plus one opening-balance query. Acceptable for a report, but it is the heaviest read in the finance module.
- [ ] Cash flow has no DB-touching spec — `listAccountSections`, `getSectionByAccountId` and `getCashOpeningBalance` are covered only by reading. The pure engine, classifier and comparison are fully unit-tested.
- [ ] **KAS is now restricted to ASET at all three chokepoints** — `setCashFlowSectionAction` refuses with `KAS_REQUIRES_ASET`, the classification combobox disables the option off ASET, and `listAccountSections` only ever offers leaves. The earlier note here had the diagnosis BACKWARDS: `debit − credit` is the orientation `netChange` and `getCashOpeningBalance` already shared, and it was the ENGINE's `row.signed` that diverged, so a LIABILITAS account marked KAS (a bank overdraft, legitimate under IAS 7) read its movement with the sign flipped and doubled the cash side — a permanent false "data jurnal rusak" alarm on a balanced ledger, not a mislabelled opening balance. The engine now accumulates `row.debit - row.credit`, so all three paths state one orientation. Residual: an override stored BEFORE this branch can still hold KAS on a non-ASET account (nothing migrates existing rows, and the option stays selectable-looking in the trigger label so it is at least visible); and supporting overdraft-in-cash properly means orienting the whole cash path off `AccountType`, not relaxing the check (feat/finance-cash-flow-statement).
- [ ] Non-cash investing and financing transactions produce phantom GROSS flows in the section subtotals: buying a vehicle wholly on credit shows investing −50.000.000 and financing +50.000.000 though no cash moved. Inherent to deriving sections from balance deltas under the indirect method — only the NET is a measured figure, and it is correct. Needs a disclosure sentence saying the section subtotals may include non-cash transactions, not code.
- [ ] `kasAkhirActual` is documented as the "measured" closing cash figure but is itself derived (`kasAwal + Σ cash-account deltas`), and it is rendered on no surface — screen, print and Excel all show `kasAkhir`. Either surface it beside `kasAkhir` as the corroborating figure it is meant to be, or drop it and let `isReconciled` be the only consumer.
- [ ] A quiet period (`hasMovement === false`) hides `kasAwal`/`kasAkhir` entirely on the cash flow screen, while the print view has no such guard and renders the full statement at zero. The two surfaces disagree about what an empty period looks like, and the screen is arguably the wrong one — opening and closing cash are the figures a reader most wants from a quiet month.
- [ ] Cash flow `to < from` produces an inverted previous window and renders the "empty period" state rather than a validation message, so an operator who transposes the dates is told there are no journals instead of being told the range is backwards.
- [ ] The cash flow print view silently omits the previous-period comparison that both the screen and the Excel show. Deliberate (three amount columns do not fit the portrait sheet) but undisclosed — the PDF should at least say it is single-period. Related to the landscape-variant item above.
- [ ] Neither `setCashFlowSectionAction` nor `clearCashFlowSectionAction` has a try/catch, so an infrastructure failure or a P2025 on a concurrently-deleted account throws out of the server action and no toast fires at all — the operator sees the row simply not change. Both already return typed result codes precisely to avoid digest-masked throws; the uncaught path defeats that.
- [ ] `loadCashFlowReport` awaits the current and previous windows sequentially though they are independent — `Promise.all` would roughly halve the latency of the heaviest read in the finance module. Same call site as the double-`getAccountBalances` item above.
- [ ] `INCOME_STATEMENT_COVERAGE_BODY` carries the SAME staleness the cash flow coverage note was just fixed for: it still names "penjualan kanvas" and "pembayaran ke pemasok" as un-journaled, but van sales post `DR CASH / CR SALES_REVENUE` (`lib/canvassing/van-journal.ts`) and supplier payments post `DR AP / CR BANK` (`lib/purchasing/supplier-payment-journal.ts`) on master today. Mirrored in `financeReports.incomeStatement.coverageBody` in both locales. Left alone on the cash flow branch because it belongs to a different report.
- [ ] The cash flow comparison table has no `min-w-*`, so on a phone the 4-column layout compresses and wraps rather than scrolling inside its `overflow-x-auto`. Matches `TrialBalanceClient`'s existing house style, so fixing it is a sweep across the finance report clients rather than a one-file change.

### Inventory — Opname, Reconciliation & Stock UI
- [x] NULL-variant `InventoryValue` lookup in opname drift/adjustment (`opname-approve.ts`) — PR #158.
- [ ] Same NULL-variant strict lookup in `reconciliation-runner.ts:76` (`applyMatchJubelio`) — misses `variantSku=NULL` rows → correction silently skipped. Dormant (`FLAG_ONLY`); fix before enabling auto-reconcile. Memory `project_inventory_null_variant_lookup`.
- [ ] GRN + Adjustments inventory tabs client-filter only the current page — push search server-side (oversold-only view is already server-side) (PR #133).
- [x] Stock opname does not cover virtual warehouses. Opname is built entirely on `InventoryValue`, so covering `StoreStock` needed a **second implementation, not a filter** — and `VanStock` sidestepped the identical gap earlier by shipping a bespoke reconcile rather than extending opname. The store stocktake is that second implementation: its own document type, its own writer, no opname code touched (feat/konsi-virtual-warehouse; feat/store-stocktake, PR #257).
- [ ] `StoreStock.avgCost`'s negative-blend clamp discards real cost: a `−5 @ 50.000` row taking a `20 @ 60.000` transfer blends against weight 0, so the row values at 60.000 where the movements total more. Contained today because that column has exactly one reader, the blend itself (feat/konsi-virtual-warehouse).
- [ ] `getStoreStockCard` fetches every transfer line and approved return line for a store with no `take` — same unbounded shape as the existing store-detail cards (feat/konsi-virtual-warehouse).
- [ ] The `getStoreStockCard` join test only proves cross-item isolation, because both fixture items use `variantSku: ""` — dropping `variantSku` from the join key would still pass. Needs a same-item multi-variant fixture (feat/konsi-virtual-warehouse).

### Jubelio integration / sync
- [ ] Wire the outbound `salesreturn_decision_push` handler — accept/reject decisions enqueue but never sync (`outbox-router.ts:34` → `HANDLER_NOT_WIRED`); blocked on Jubelio resolve-endpoint docs (PR #58).
- [ ] New-product first push emits empty `variation_images` (built only for SKUs with an existing mapping) → a second push is needed; auto-self-enqueue after upsert (PR #75).
- [ ] `ProductPushHandler` DELETE-variant path (`:138`) is two bare awaits (http.delete then deleteMany), no tx guard → HTTP-ok + DB-fail drift; contrast the tx-wrapped upsert path (PR #42).
- [ ] Stock webhooks refetch group detail N× for N same-group events (`stock.handler.ts:62`) — coalesce/batch by `item_group_id` (PR #78).
- [ ] Add a `source:"JUBELIO"` filter to `consumeOrder`/`releaseOrder` (defense-in-depth; currently scoped only by `salesorderId`) (PR #98).
- [ ] `buy_price` stays global (decision H3) — revisit if Jubelio's `buy_price` starts feeding marketplace reporting (PR #52).
- [ ] Tokopedia settlement adapter (`TT-` prefix) unsupported — `match-key.ts` is Shopee-only (#19).

### Settlement reconciliation (Jubelio salesorder resync)
- [ ] Guard the raw `POST /jubelio/salesorders/resync` endpoint — the web action path is HMAC-signed (`internal-sign.guard`) but the controller has no guard, so the raw route is open. Add before the bulk backfill (PR #165).
- [ ] Resync `no→id` resolve + the settlement-line `q` lookups depend on the **undocumented** Jubelio `q` search (`GET /sales/orders/?q=`, `/completed|cancel|failed/?q=`) — the OpenAPI spec lists only `page`/`pageSize`, and the documented `POST /wms/order/getOrderByNo/` is pick-flow-scoped (200-empty for settled orders). Brittle if Jubelio changes it; no contract guarantee (PR #165).
- [ ] Resync `attempts` counter under-reports real retries (BullMQ owns the retry count via `attemptsMade`; the DEAD decision is correct, this is just an observability gap) (PR #165).
- [ ] `JubelioResyncModule` re-declares `SalesOrderWebhookHandler` + `JubelioWebhooksService` as providers (their home modules don't export them) → two stateless-singleton instances app-wide; harmless today, would diverge if either gained state (PR #165).
- [ ] Resync progress-panel re-attach is per-browser (localStorage keyed by settlement id) — cross-device / multi-operator re-attach would need a `settlementId` link on `JubelioSalesOrderResync` + a "latest in-flight batch" query + migration (PR #165).
- [ ] Variant-key derivation mismatch surfaced during resync ingest — reserve looked for `27000101P-XL` while the item code is `27000101P-BLK-XL`; couldn't confirm on `:3308` (0 inventory rows for the item). Verify the reservation-writer variant derivation on prod before the bulk (PR #165).
- [ ] Settlement-line `netIncome` is the excel value; a RETURNED order can legitimately show excel net 0 vs Jubelio escrow > 0 (return leg) — the Sub-B compare flags it correctly, but decide the P&L "truth" for returned orders (excel realized-0 vs Jubelio gross escrow) (PR #166).
- [ ] Settlement compare `differCount` counts only computable deltas — can read "0 differ" while most lines are simply not-yet-compared (pre-resync). Add a "N belum dibandingkan" bucket / tooltip (PR #166).
- [ ] Settlement compare selects + ships the full `raw` sheet-row JSON to the client but never renders it — drop from the query until a view reads it (payload weight) (PR #166).
- [ ] Settlement compare could surface Jubelio's fee-by-fee breakdown (`service_fee`/`order_processing_fee`/escrow object from `feeBreakdown`) beside the excel fees, not just the net delta — data already stored; HPP/profit would need the item cost (populates on prod, else product-master fetch) (PR #166 follow-on).
- [x] Settlement compare `jubelioNet` = `feeBreakdown.escrow_amount` basis — VALIDATED live: on real orders the excel net == Jubelio escrow (e.g. 201.815==201.815, delta 0), and a returned order's escrow matched the dashboard 202.910 (PR #166).

### TikTok/Tokopedia settlement adapter
- [x] VALIDATED end-to-end on the real TikTok income export (18-line settlement): parser parses (checksum matched), Decision #2 net column (`Jumlah penyelesaian pembayaran`) is correct — 2 completed orders matched Jubelio escrow exactly (208.073, 256.730), 16 canceled reconciled at 0 → 18/18 Matches, 0 differ (PR #168).
- [x] TikTok escrow lives at `escrow_list.settlement_amount` (top-level `escrow_amount` is null for TikTok) — `buildFeeBreakdown` now falls back to it; the compare's residual absorbs `fee_and_tax_amount`/shipping (PR #168).
- [x] TikTok/Tokopedia `salesorder_no` is `TT-<ref>-<suffix>` / `TP-<ref>-<suffix>` — the resolver matches `ref_no` (the bare id the excel/settlement keys on) OR `salesorder_no`, so both resolve; Shopee still matches `salesorder_no` (PR #168).
- [ ] TikTok `Total Biaya` sign — normalized with `Math.abs()` (journal-safe); held on the real export (residual reconciled, journal checksum passed). Drop the abs only if a future export proves the sign (PR #168).
- [ ] TikTok period dates derived by scanning for Date-typed cells (min/max), falling back to today if none — the real export DID carry dates (period resolved to a real range, not today), so low-risk; revisit if a dateless export appears (PR #168).
- [ ] TikTok checksum (`parsedNetTotal == totalDilepas`) is tautological (both `Σ netIncome`), unlike Shopee's cross-source check (PR #168).
- [x] TikTok "Other Adjustments" residual itemized into **Fee & Tax** (`escrow_list.fee_and_tax_amount`) + **Shipping Cost** (`escrow_list.shipping_cost_amount`) — captured in `buildFeeBreakdown`, residual → ~0 (PR #168).
- [ ] Shopee residual (the remaining "Other Adjustments") is `escrow_list.seller_transaction_fee` for the sampled order, but Shopee's `escrow_list` has many fee fields and which ones deduct to `escrow_amount` varies per order — kept as a reconciling residual by choice (not itemized). Revisit only if per-line Shopee fee detail is needed; would need per-order field mapping (PR #168).
- [ ] `TOKOPEDIA` (`TP-` orders) is wired in `match-key.ts` + resolves by ref_no, but has **no income-export parser** — a Tokopedia settlement upload fails at the parser dispatch until a Tokopedia adapter is added (its sheet/columns likely differ from TikTok's). Supersedes the old "Tokopedia adapter unsupported (#19)" item (PR #168).

### Sales returns
- [ ] `SalesReturn.jubelioReturnId` actually stores `salesorder_id` — misleading name, rename deferred (migration churn) (PR #55).
- [ ] 100 of 102 dev `SalesReturn` rows have `salesOrderId IS NULL` and CANNOT ever link: their `jubelioReturnId` values span 1–2586 while local `SalesOrder.salesorderId` spans 24688–743834598 — disjoint id spaces. Jubelio's own payload reports the same low ids, `jubelioReturnNo` matches no `salesorderNo`, and all 100 were written in one 50-second window on 2026-07-02; the 2 rows that DID link sit inside the real sales-order id space. Looks like a one-off import or test artifact rather than 100 genuine returns — decide what they are before treating them as data to repair, because under the return-journal gate every one of them is permanently unpostable (PR #222).
- [ ] `SalesReturn.salesOrderId` is declared under `relationMode = "prisma"` (no DB-level FK) but the live `:3308` database HAS a real `SalesReturn_salesOrderId_fkey` constraint — schema declaration and live DB disagree. Found when a dangling-FK test failed against the real constraint. Worth a wider audit of which `relationMode` relations actually carry DB constraints (PR #222).
- [ ] Same `relationMode = "prisma"` vs real-FK divergence on the field-sales side, and it is now load-bearing: `FieldSalesOrder.storeId`/`approvedById` and `FieldSalesOrderLine.itemId` carry NO database constraint, while `FieldSalesDelivery.deliveredById` and `FieldSalesDeliveryLine.itemId` — the columns the backfill migration writes INTO — carry real enforced ones. A dangling source reference is invisible until it surfaces as a foreign-key violation mid-migration, which is exactly what that migration's pre-flight exists to catch. The wider audit is still unrun (feat/field-sales-delivery).
- [x] The self-aborting SQL pre-flight pattern is **PROVEN** and reusable — `SELECT IF(<count> = 0, 'preflight ok', (SELECT 'ABORT' UNION ALL SELECT 'ABORT'))` aborts the migration with ERROR 1242 on a non-zero count and returns cleanly otherwise. Verified 2026-08-09 on both the prod server and the `:3308` bed (MariaDB 11.4.12): MariaDB evaluates only the taken branch (so it does NOT fire on a healthy database), and **`prisma migrate deploy` both tolerates a result-set-returning `SELECT` inside a migration file and propagates its error as a failed migration**. That second half was the part no amount of reading could settle. Reuse this instead of a `--` comment nobody reads when a data migration must not run against unsafe rows; note that an abort leaves the migration recorded failed, so recovery is `prisma migrate resolve --rolled-back <name>` before re-running (PR #229).
- [ ] Return evidence R2 mirror (R5) — `evidenceUrls` exists in schema but the ingest never assigns/mirrors it (not even ingested) (PR #54/#55).
- [ ] Extract `JubelioHttpService` into a leaf module to break the triple-hop forwardRef circular (Jubelio↔Queue↔Returns) (PR #54).

### Field Sales / PWA (SFA)
- [ ] Price appeal: `approveFieldSalesOrderAction` `finalPrices` is **UI-gated only** — the server doesn't reject a stray `lineId` (silently no-ops) nor require a final price for every appealed line (silently falls back to the store price = appeal denied). Safe fallback + permission-gated, but add server-side enforcement (lineId ∈ order + require an entry per appealed line) before treating it as a hard guarantee (feat/field-sales-price-appeal).
- [ ] Store-detail cards (Visit history, Items Sent) are scroll-bounded (`max-h-96` + sticky header) but **unpaginated** — Visit history caps at 50 (`listVisitsForStore`), Items Sent is an all-time aggregate with no cap. If a store accumulates hundreds of article×size rows / visits, add server-side pagination or an in-card search (`StoreDetailView.tsx`, `getStoreSentItems`) (feat/store-sent-items).
- [x] Putus: server-recompute `unitPrice` — DONE (#207). Putus create computes `unitPrice` via `computeStorePrice` PUTUS in the `linesData` map (`field-sales/writer.ts`), ignoring the client-sent price; the client price is display-only. Superseded the "trusts client price" debt.
- [ ] Putus oversell is WARN-not-block at create; approve doesn't re-surface short stock — unresolved design call (#21).
- [ ] Offline: `newLocalId` double-tap can create 2 orders (`CatalogShell.tsx:165`; `idempotencyKey` only dedups replay of the same id) (#21).
- [ ] Offline: logout (`pwa/actions.ts`) clears neither the Dexie order queue nor the SW page cache (one-salesman-per-device) (#21).
- [ ] Photos: 401 is terminal (`photo-classify.ts:3`) → marked `failed` (consider treating auth as transient); soft-cap is client-only, upload route has no server cap (#21).
- [x] Konsi: real stock transfer to store virtual warehouse + surat keluar — DONE. Approving a konsi order now physically moves stock into a per-store `StoreStock` ledger and the surat keluar print moved onto the new `KonsiTransfer` document (branch `feat/konsi-virtual-warehouse`). See `docs/ARCHITECTURE-NOTES.md` and `docs/EPIC-STATUS.md` § Inventory Extensions.
- [ ] Canvassing: offline write-queue for van sales, promos on van sales, void/refund — all deferred (#21).
- [ ] Territory model — every salesman sees every active store (`listActiveStoresForPwa` has no salesman filter) (#21).
- [ ] PWA i18n debt: hardcoded ID strings in `CatalogShell.tsx:180,192`, `VisitPhotoCapture.tsx:139-190`, lightbox `StoreDetailView.tsx:415,439` (pending-orders view already i18n'd) (#21).
- [ ] Extract a shared `QtyStepper` (inline-duplicated across catalog/van sheets) + finish `lib/sales-orders/format.ts` adoption (~9 sites still inline `Intl.NumberFormat`, plus seven copies of `formatRupiah` across the three finance report clients, `ReportSection`, and the three finance print pages — the print copies are deliberately isolated, the client ones are not); cross-page nav pending-state; CI `tsc --noEmit` gate (#21).
- [ ] Field retur: the register drops the time of day — two returs raised for the same store on the same day are indistinguishable in the list, so an admin reconciling a phone call ("the one I sent this morning") cannot tell them apart without opening both (feat/field-retur-request).
- [ ] Field retur: a variant item whose inventory happens to be a single POOLED row files its line as `variantSku: ""`, which violates the per-variant contract the rest of field sales holds to. The warehouse then receives a claim that names an article but not a size (feat/field-retur-request).
- [ ] Field retur: the retur screen's catalog fetch reuses the shared `/pwa/api/catalog` route, so it DOUBLES the already-unbounded `pwa-catalog` SW cache entry per store, and ships pricing fields the retur screen never renders. A retur-shaped payload (or a distinct cache key) would fix both (feat/field-retur-request).
- [ ] Field retur: `keepPhoto` swallows a compression failure and only leaves the overlay open — the salesman gets no message saying why "Simpan" did nothing (feat/field-retur-request).
- [ ] Field retur: the `includeInactive` param added for the retur picker is open on the SHARED `/pwa/api/catalog` endpoint, and `createFieldSalesOrder` never checks `Item.isActive` — so a salesman can order a discontinued item by fetching the catalog with that flag set. Either scope the param to the retur caller or add the `isActive` check to the order writer (feat/field-retur-request).
- [ ] Field retur: no `loading.tsx` for the `/pwa/stores/[id]/retur` segment, unlike the catalog segment beside it — the screen shows nothing until the client fetch resolves (feat/field-retur-request).
- [ ] Field retur: the variant-picker trigger is a 32px tap target, under the 40px minimum the rest of the PWA holds to (feat/field-retur-request).
- [ ] Field retur: the backoffice register has a search box but no status filter, so `CANCELLED` returs cannot be excluded from the working list (feat/field-retur-request).
- [ ] Field retur receiving: the backoffice nav entry is gated on `PERMISSIONS.FIELD_SALES_ORDERS_VIEW`, and neither the register nor the detail page carries a `field_returns:*` gate — so a role granted only `field_returns:manage` gets no nav item and must know the URL. The design's intent was that warehouse-only access becomes a config change rather than a code change; it is still a code change (feat/field-retur-receiving).
- [ ] Field retur receiving: `SPLIT_MISMATCH` does not name the offending line, though the design's error table says it should — `FieldReturnError` carries a bare code and both locale strings are generic. The diagnosis is right, just less specific than promised (feat/field-retur-receiving).
- [ ] Field retur receiving: a mistyped count is uncorrectable — receiving is once-only by design, so the only remedy for a fat-fingered quantity is hand-run SQL before approval (feat/field-retur-receiving).
- [ ] Field retur receiving: `getFieldReturnById` loads a line's `resolutions` with no `take`, so a heavily-amended line ships its whole history to the client (feat/field-retur-receiving).
- [ ] Field retur receiving: the approve button and the resolution controls use independent `useTransition` flags, so an operator can confirm a resolution and click the still-enabled approve before the first round trip resolves, firing approve against a soon-stale settled-read. The server transaction guards correctness; the UI does not prevent the double-fire (feat/field-retur-receiving).
- [ ] Field retur receiving: no inline error text when a required note is left blank on `INVESTIGATE`/`ACCEPT_SURPLUS` — only a disabled confirm button and a "(Wajib)" label signal it (feat/field-retur-receiving).
- [ ] Field retur receiving: the latest-resolution ordering `[{ createdAt: "desc" }, { id: "desc" }]` is spelled in three places (the resolve writer, the approve writer, and `getFieldReturnById`) with no shared home, because the one module that could hold it must stay import-free. Each copy is now covered by a test; the duplication is not (feat/field-retur-receiving).
- [ ] Field retur mismatch notification: only the ADMIN half is built. The salesman half is unbuilt because **the PWA has no notification surface whatsoever** — the bell (`NotificationIcon`) and `FcmRegistration` are mounted only in `BackofficeShell.tsx`, so a salesman has nowhere to see a notification and no registered FCM token. Separately, `fanOutAdminNotification` resolves recipients by PERMISSION, not by user, so "tell the salesman who raised this document" has no existing mechanism either — `sendNotificationToUsers` in `lib/notifications/recipients.ts` can target a user list, but nothing calls it that way. Building it means a PWA notification list, FCM registration plus its browser permission prompt, and a targeted-send path. It would unblock several other "tell the salesman" stories: order approved or rejected, store change request decided, retur received (feat/field-retur-mismatch-notification).
- [ ] Field retur mismatch notification: this new `FIELD_RETURN_MISMATCH` category feeds a bell with a known hard ceiling — `app/api/notifications/route.ts` has `LIMIT = 50` with no cursor and no load-more, there is no mark-all-read, and nothing prunes `NotificationQueue`, so past 50 unread rows everything older is unreachable and the badge can never return to zero. That ceiling is pre-existing recorded debt (see Notifications follow-ups); adding a category feeds it (feat/field-retur-mismatch-notification).
- [ ] Field retur value: the register's "incomplete" marker only fires on `APPROVED + PENDING` rows (a permanent, un-fixable gap) — a still-open retur whose lines are `AMBIGUOUS`/`UNPRICEABLE` and COULD be fixed via `LinePriceControls` right now gives no signal from the list at all; an admin only discovers it by opening the retur. Worth a second, distinguishable marker for the actionable case (feat/field-retur-value).
- [ ] Field retur value: once a retur is `APPROVED`, `priceState` can no longer distinguish `AMBIGUOUS` from `UNPRICEABLE` on a line that never got priced — `getFieldReturnById` stops computing `priceCandidates` for a closed retur, so both collapse to `UNPRICEABLE`. The unpriced count stays numerically correct, but the reason (deliveries disagreed vs. never delivered at all) is lost the moment approval happens (feat/field-retur-value).
- [ ] Field retur value → settlement link is UNBUILT and BLOCKED, not deferred UI polish: there is no AR, receivable or settlement model anywhere in this system for an approved retur's value to auto-populate into. The `APPLIED` marker was deliberately left off the schema too — a column nothing writes is worse than no column. Needs a Finance/AR or settlement epic before this story is buildable at all (feat/field-retur-value).
- [ ] Admin store return: the in-transit window vs. the stocktake. An admin-origin retur decrements `StoreStock` at RECEIPT, not at raise, so between raise and receipt the store ledger still counts units that are physically on a truck. A stocktake opened in that window reports a shortfall that is really goods in transit. The store card shows the in-transit quantity, but the stocktake count screen does not and must not — `expectedQty` is defined as the live ledger figure and cannot gain a second meaning without breaking the stocktake's own contract (feat/admin-store-return, PR #258).
- [ ] Admin store return: `SALESMAN_BEARS`/`creditedQty` are vocabulary that now only half-applies. Both read as "the salesman is charged", which is meaningless on an admin-raised return — the writer refuses `SALESMAN_BEARS` on `origin: ADMIN` (`resolve-writer.ts`), but `creditedQty` keeps a name that describes only the FIELD origin it was coined for (feat/admin-store-return, PR #258).
- [ ] Admin store return: `ReturShell.messageForFailure` (PWA) falls through the two new codes — `MISSING_NOTA_PHOTO` and `MISSING_TRANSPORT` both hit the generic "Gagal menyimpan retur" instead of a specific message. Only reachable via a malformed client request on the FIELD-only PWA route (an admin return never sends these fields), so a UX gap, not a build break (feat/admin-store-return, PR #258).
- [ ] Admin store return: `raiseAdminReturnAction`'s negative-row test is tautological — the action never reads `StoreStock` itself, so a mocked writer returning success only proves the mock works. The real coverage of the origin-dependent decrement is the DB-backed writer test in `admin-origin.test.ts` (feat/admin-store-return, PR #258).
- [ ] Admin store return: no runtime rejection of an out-of-union `origin` value — relies on TypeScript narrowing at the call site; Prisma's enum column would reject an invalid value at the database anyway. Low risk (feat/admin-store-return, PR #258).
- [ ] `CANCELLED` is still an unwritten `FieldReturnStatus` member — no cancel/reject writer exists for field returns at all (FIELD or ADMIN origin), so an abandoned admin-raised return has no disposal path once raised. Noticed while re-entrancy-checking the admin origin path (feat/admin-store-return, PR #258).
- [ ] SPG: no UI to set `User.assignedStoreId` — must be set via DB today; add an assigned-store picker on the user form (#208).
- [ ] SPG: offline write-queue for sales (deferred, mirrors the van-sales offline debt); promos on SPG sales; sell-through reconciliation reporting — a periodic SKU+qty-sold batch report is still a different, unbuilt thing from the store stocktake's physical count (#208).
- [x] SPG: `StoreStock` now backs a real per-store ledger — the konsi transfer, a konsi retur approve and a KONSI-store SPG sale all write it — and a drifted or negative row is now correctable by opening and approving a store stocktake, though nothing schedules, prompts or reminds anyone to open one (#208, #257).
- [ ] Every consignment store an SPG sold at before the KONSI-store `StoreStock` decrement shipped was carrying an inflated `StoreStock` row — the decrement only stopped the overstatement from growing further, it did not repair what had already accumulated. The store stocktake (feat/store-stocktake, #257) now gives that repair a path — approving a count overwrites `StoreStock.qty` with the physical figure — but the CAPABILITY landing is not the DATA getting fixed: no store's drift is actually repaired until someone opens and approves a count there, and nothing schedules, prompts, or reminds anyone to do so. Every uncounted store's `StoreStock` row is exactly as inflated today as before this shipped. Reserve the tick for a measured backfill (or a completed sweep of every affected store), not for the correction path merely existing.
- [ ] Store stocktake: nothing schedules, reminds or prompts a stocktake. A drifted or negative `StoreStock` row is only ever repaired at a store where someone chooses to open a count — the feature makes correction POSSIBLE, not automatic (feat/store-stocktake).
- [ ] Store stocktake: a partial count leaves every uncounted row exactly as drifted as it was. `isFullCount` records which count was partial, but nothing chases the uncounted lines afterward (feat/store-stocktake).
- [ ] Store stocktake: the four-table "how much of this item exists" fan-in (`InventoryValue`/`VanStock`/`StoreStock`/now stocktake lines) is still only folded by `getStockAcrossLocations`, and remains unproven against a store that has actually been counted (feat/store-stocktake).
- [ ] Store stocktake: approval writes no `StoreStockMovement` row — there is no such ledger, so documents ARE the audit trail for store-side stock, same as the konsi retur decrement above. The board's "all movements create audit trail entries" criterion stays PARTIAL BY DESIGN, not by omission (feat/store-stocktake).
- [ ] Store stocktake: `createStoreStocktake`'s P2002 catch does not inspect `e.meta.target`, so a collision on a different unique constraint inside the same create would also report `ALREADY_OPEN`. Practically unreachable — `docNo` comes from an atomic counter in the same transaction (feat/store-stocktake).
- [ ] Store stocktake: the within-batch duplicate-add test cannot distinguish the `seenAddedKeys` pre-check from the P2002 fallback catch — both roll back identically. Tests external behaviour only (feat/store-stocktake).
- [ ] Store stocktake: the add-item picker fetches the whole active-item catalog unpaginated, on BOTH the backoffice count screen and the PWA sheet. Fine at current catalogue size (feat/store-stocktake).
- [ ] Store stocktake: `getStoreStocktakeById` and `listStoreStocktakes` have no direct tests. They are thin schema-mirrored reads and the UI is their only consumer (feat/store-stocktake).
- [ ] Store stocktake: a concurrent SPG submit race can surface `ALREADY_OPEN` to a PWA whose UI has no concept of an open document. Cosmetic — no duplicate document is created — and documented in a code comment at the site (feat/store-stocktake).
- [ ] PWA tap-target sweep: `SpgHomeShell`'s "Catat Penjualan" CTA (both the enabled and the disabled/check-in-first variant) is still h-9 (36px). The stocktake entry CTA beside it and the stocktake success screen's "Kembali ke Beranda" were brought to 40px on this branch; "Catat Penjualan" was left alone deliberately to avoid forking the shared button pattern mid-branch (feat/store-stocktake).
- [ ] `apps/web/lib/docNumber.ts` and `apps/web/app/actions/settings/doc-numbers.ts` are single-quoted files, against the repo-wide double-quote convention. Pre-existing; the new `STOCKTAKE` lines matched the file (feat/store-stocktake).
- [ ] Pack-ratio: the Settings card client re-implements `validateRows` instead of importing the client-safe `validatePackRatio` from `@elorae/db/pack-ratio` — exact parity today (server backstops), but a thin wrapper would kill the drift risk (#210).
- [ ] Pack-ratio: `settings_pack_ratio:view` defined but unreferenced (`getPackRatio` un-gated, matching the `getPpnRatePercent` sibling convention) — wire it if the page ever needs a read gate (#210).
- [ ] Smart-request: `buildSmartRequestAction` is auth-only — doesn't fail-fast on a missing/foreign active visit (build is read-only; submit's writer enforces the visit). Add a visit check to fail fast off-visit (feat/smart-request).
- [ ] Smart-request: no server-side KONSI guard in build/submit — putus-only is enforced by the page redirect + PUTUS-gated CTAs, and the writer re-prices a konsi store at submit, so no integrity risk; add a defensive `termsType==="KONSI"` reject in the actions for clarity (feat/smart-request).
- [ ] Smart-request: a selected category with zero candidate articles yields `underfill>0, lines=0, dropped=0` → renders the generic "tidak ada produk" empty state without the underfill note (cosmetic; same outcome conveyed) (feat/smart-request).
- [ ] Konsi suggestions: candidates with zero or NEGATIVE availability are listed with no filter and no sort — every never-sent variant appears, out-of-stock ones only distinguished by a disabled stepper, and the list is sorted by SKU alone. On a large catalog the admin scrolls past unusable rows to reach the sellable ones. Wants an availability sort (or a "hide out of stock" toggle), which is also the cheapest fix for the negative-availability row printing a bare `-3` (feat/konsi-never-sent-suggestions).
- [ ] Konsi suggestions: an added item whose `Item.sellingPrice` is `null` prices at 0 through `computeStorePrice`, so it transfers at zero value with no warning anywhere — not in the panel, not in the approve dialog, not on the order detail. The PWA catalog already has the idiom for this ("Harga belum diset" tag on an in-stock unpriced item); the panel should carry the same tag, and arguably refuse to stage it (feat/konsi-never-sent-suggestions).
- [ ] Konsi suggestions: `apps/web/app/backoffice/field-sales-orders/[id]/loading.tsx` is hardcoded English on a page that is otherwise fully translated, so an `id`-locale admin sees an English skeleton flash (feat/konsi-never-sent-suggestions).
- [ ] Konsi suggestions: `konsi-added-lines.test.ts` leaks two `AdminNotification` rows per test (~30 per run, and it grows with every case added) — `createFieldSalesOrder` writes one per seeded order and the teardown never deletes them (the fan-out itself is mocked, so nothing reaches `NotificationQueue`). Harmless but it accumulates on the shared `:3308` bed, and the bell's hard 50-row window is already a known problem. Delete them in `afterEach` scoped to the seeded order ids (feat/konsi-never-sent-suggestions).
- [ ] Konsi suggestions: the `onOrder` exclusion in `listKonsiSuggestions` is dead code — `sentItemIds` covers non-REJECTED konsi lines, which includes the PENDING_APPROVAL order being approved, so every item on the order is already excluded by `sent`. The writer's own `onOrder` check is NOT dead (it must fire before `alreadySent` to return `DUPLICATE` rather than `ALREADY_SENT`), but the query's is, and its test cannot distinguish the two paths. Either drop it or add a case only it can satisfy (feat/konsi-never-sent-suggestions).
- [ ] **Store assortment — no admin-initiated push transfer exists, and this is the single biggest limit on the feature's usefulness, not a footnote.** `konsiTransfer.create` is called from exactly one place, `approveFieldSalesOrder` — there is no way for an admin to act on an assortment gap directly. The assortment records intent ("this store should carry this"); nothing in this feature lets anyone DO anything about a gap except wait for a salesman to independently raise a konsi order at that store and hope the suggestions panel's gap rows catch it. A store with no active salesman, or a salesman who never opens a konsi order there, has its gaps sit forever with no path to close them (feat/store-assortment).
- [ ] Store assortment: templates and bulk copy between stores were deliberately deferred. `Store` has no region, tier, category or chain field, so there is no hierarchy to hang a shared assortment on, and inventing one now would be building a structure nobody asked for. Revisit if maintaining several similar stores' assortments by hand actually starts to hurt (feat/store-assortment).
- [ ] Store assortment: nothing reminds anyone to maintain an assortment as demand shifts. It goes stale silently — no review prompt, no staleness signal, no usage-based suggestion — same unscheduled-maintenance shape as the store stocktake above (feat/store-assortment).
- [ ] Store assortment: adding a line while a stocktake is already OPEN mislabels it. The SKU appears on the SPG count sheet (via the assortment prefill) but has no row on the already-open document, so `saveCountsAction` folds it into `fallbackAddedLines` and it persists with `isAdded: true` — the backoffice then shows an "Added" badge on a line the store was always supposed to stock. Pre-existing shape, identical to a `StoreStock` row appearing mid-count (documented at `apps/web/app/actions/store-stocktakes.ts:288-297`), but this feature makes it materially likelier since assortment lines are added independently of any open count (feat/store-assortment).
- [x] Store assortment: the two test gaps parked during the actions task are closed — a genuine `(id, storeId)` mismatch and a gates-nothing surface that pins exact call counts on all three mocks (#259). `apps/web/app/actions/store-assortment.spec.ts` has a genuine id/storeId MISMATCH test for both `updateAssortmentTargetAction` and `removeAssortmentLineAction` (a real line under a different store, via a mock simulating compound-where AND semantics), and the "gates nothing" test now pins exact prisma call counts so a gate bolted onto either touched model would be caught, not just a plain `ok === true` (feat/store-assortment).
- [ ] Nit: `apps/web/lib/stores/stocktake/queries.test.ts` has one new test asserting a raw `itemAId` while its sibling in the same file uses `seededId(itemBId)`. Harmless, just inconsistent with the `seededId()` convention (feat/store-assortment).
- [ ] Store price discount: `StorePrice.flagged` has NO consumer anywhere in `apps/web`. All eight `computeStorePrice` call sites destructure `price` only, so the helper's "flag an out-of-range percent and return the unadjusted price" behaviour surfaces to nobody — a bad stored value would silently charge full list. The write-boundary guard (`assertValidPriceDiscount`) is the ONLY thing that actually catches it. This predates the discount (the KONSI bad-margin path has always flagged into the void) but the discount doubles the number of ways to reach it. Either give `flagged` a consumer or stop pretending it is protection (feat/store-price-discount).
- [ ] Store price discount: a nonexistent `storeId` is treated as "no discount" rather than an error in `recordVanSale` and `getSellableCatalogForSpg` — both do a `findUnique` and fall back to null. Pre-existing shape, not introduced by this feature, but it means a bad id prices at full list silently (feat/store-price-discount).
- [ ] Store price discount: cosmetic, parked from the store-detail-form review — `StoreForm.tsx`'s `onValueChange` carries a two-line comment in neither the flat nor the starred JSDoc comment form; and the KONSI notice uses `text-xs` where the `StoreAssortmentCard` notice it mirrors uses `text-sm` (feat/store-price-discount).
- [ ] Delivery: `consumeFieldSalesOrder` (whole-order) has NO production caller left — the delivery path uses `consumeFieldSalesOrderPartial`. Anyone reaching for the old one bypasses the delivery document entirely (no `FieldSalesDelivery` row, no per-delivery `SalesHistory`, `deliveredQty` never moves). Either delete it or rename it so the trap is visible (feat/field-sales-delivery).
- [ ] Delivery: `StockReservation.consumedQty` means two different things by source. The field-sales partial consume maintains it; `consumeOrder` (Jubelio) never sets it, so a JUBELIO row reads `consumedQty = 0` while `state = 'CONSUMED'`. Any future reader must branch on `source` or it will conclude Jubelio reservations were never consumed (feat/field-sales-delivery).
- [ ] Delivery: `onHandByKey` in `getFieldSalesOrderById` SUMS the `variantSku` null and `""` inventory rows for a key, while `findFieldSalesInventory` (the writer's lookup) picks the FIRST of them. On an item that somehow has both rows, the delivery form's on-hand cap is larger than the row the consume will actually decrement — the form offers a quantity the writer then refuses with `INSUFFICIENT_STOCK`. **The PR #229 review bot rated this a live form↔writer contract bug rather than a theoretical dual-row edge case, and it is right**: the two paths disagree by construction, not only when data is malformed. Fix by aligning the read path with the writer (same first-row rule), or by merging the dual rows before any cap is exposed (feat/field-sales-delivery, PR #229). **It now has a SECOND consumer:** the konsi never-sent panel auto-expands off `shortLineCount`, which is derived from the same summed `available`, while `listKonsiSuggestions` deliberately takes the MINIMUM of a dual row — so on a dual-row item the panel can stay collapsed for a line the writer will refuse, and the admin is given no hint that the suggestions are the remedy. Fixing the read path fixes both surfaces at once (feat/konsi-never-sent-suggestions).
- [ ] Delivery: the `INSUFFICIENT_STOCK` error reports the on-hand it read MID-TRANSACTION, after earlier lines in the same call already decremented. The transaction then rolls back, so the number the operator is shown (and that the form snaps the input down to) is not the on-hand that exists afterwards — it is lower. Harmless understatement, but the toast is stating a figure that is never true (feat/field-sales-delivery).
- [ ] Delivery: `consumeFieldSalesOrderPartial` filters reservations by `fieldSalesLineId` + `state` but NOT by `source: "FIELD_SALES"`, unlike `releaseFieldSalesOrder` beside it. Nothing else writes `fieldSalesLineId` today so it is unreachable, but the sibling's defence-in-depth is missing (feat/field-sales-delivery).
- [ ] Delivery: `logPrint` is called with the order-era entity types `"FieldSalesNotaGudang"`/`"FieldSalesNotaTagihan"` but now passed a DELIVERY id, so the audit log's entity ids silently changed meaning for those two types. Any historical row predates the change and points at an order (feat/field-sales-delivery).
- [ ] Delivery: the nota tagihan is built with `appliedPromoName`/`appliedOrderPromoName` hardcoded `null`, so a discounted delivery prints the discount figure with no promo name beside it — unlike the order detail, which names it (feat/field-sales-delivery).
- [ ] Delivery: `deliveryStatus` is absent from the field-sales-orders LIST page — no column, no filter — so finding the orders still awaiting delivery means opening them one at a time. The detail page is the only surface that shows it (feat/field-sales-delivery).
- [ ] Delivery: the price-appeal → `SalesHistory` path lost its only cover when this branch deleted the approve-time history assertions; nothing now asserts that an owner-set final price reaches `SalesHistory` through the delivery path (feat/field-sales-delivery).
- [ ] `deleteItem`'s link checks never look at `FieldSalesOrderLine` or `FieldSalesDeliveryLine`, so an item referenced only by field-sales documents passes the "safe to delete" check and the delete then fails on the real FK (or, for the `relationMode = "prisma"` relations, succeeds and dangles) (feat/field-sales-delivery).
- [ ] Delivery: `getStoreOrderSummary` still reports `FieldSalesOrder.total` on the store profile and the PWA "Pesanan terakhir", which overstates a partly-delivered or remainder-closed order — nothing recomputes the order total. `getStoreSentItems` and the dashboard realised bucket were fixed; this one was left (feat/field-sales-delivery).
- [ ] **Delivery — highest-priority of this batch:** an APPROVED putus order awaiting its first delivery is in NEITHER dashboard bucket. Realised counts deliveries, outstanding counts orders awaiting approval, so **every newly approved putus order disappears from both cards until someone delivers it** — which reads as data loss to ops, on day one of the feature. Either widen outstanding to "ordered but not yet fully delivered or closed", or add an explicit third bucket (feat/field-sales-delivery, PR #229).
- [ ] Delivery: `recordDeliveryAction` returns `OVER_DELIVER` for a malformed payload — a non-string `orderLineId`, a non-integer or non-positive `qty` — so a client/shape error surfaces to the operator as "Delivered quantity exceeds the outstanding quantity", which is simply untrue and misleads anyone debugging it. `closeRemainderAction` does the same with an empty `reason`, returning `INVALID_STATE` ("cannot be delivered in its current state"). Both should return `INVALID_REQUEST`, which already exists on `DeliveryActionResult` and already has strings in both locales (feat/field-sales-delivery, PR #229).
- [ ] Delivery: `recordFieldSalesDelivery` maps every non-`INSUFFICIENT_STOCK` `PartialConsumeError` — notably `OVER_CONSUME` — onto `OVER_DELIVER`. That collapses the one diagnosis that matters for the deploy-window case: an order approved by the OLD image after the backfill migration has already run is stock-consumed with its reservation `CONSUMED`, receives no `bf_` delivery, and then reads as `PENDING` with full outstanding under the new app. Delivering it hits `OVER_CONSUME`, and the operator is told "quantity exceeds outstanding" with no hint that the order is stuck behind a deploy race. Surface a distinct reason so the remedy (hand-run backfill statements 2–6 scoped to that order id) is discoverable (feat/field-sales-delivery, PR #229).
- [ ] Delivery: the doc comment on `allocateDeliveryDiscounts` (`lib/field-sales/delivery/plan.ts`) says stranding needs three chunks on one line plus a fourth delivery. That holds only for an INTEGER discount — the promo engine does not round (`PERCENT` is a bare `(lineTotal * value) / 100` into a `Decimal(15,2)`), so a fractional `discountAmount` strands its sub-rupiah part on a TWO-delivery order whenever the discounted line finishes on the non-closing one. The fold absorbs it correctly and the totals stay exact; only the comment understates the trigger, and a cheaper regression test exists than the four-delivery one (feat/field-sales-delivery).
- [ ] Delivery: the closing delivery's `total` can in principle go negative, because it absorbs every stranded remainder plus the order-discount remainder while carrying only its own goods. Bounded by roughly 0.5 × chunks-per-stranded-line plus fractional parts, so it only bites when the closing delivery is worth a rupiah or two — unreachable at realistic prices, but it would print a negative grand total on the nota tagihan and push a negative `SalesHistory.orderTotal` (feat/field-sales-delivery).
- [ ] Delivery: `getStoreSentItems`'s delivered branch has no `orderType` filter. Safe today because `recordFieldSalesDelivery` refuses non-PUTUS, so only putus orders can carry deliveries — but if konsi ever gains a delivery document, a konsi order would be counted twice, once through its delivery and once through the approved-konsi union beside it (feat/field-sales-delivery).
- [ ] Nota dates are entered by the operator and are no longer derived from `Store.paymentTempo` (the client's locked answer to the order-processing due-date question). `computeDueDate` survives as a client-side suggestion behind the "Pakai tempo" button. **Deliveries recorded before this carry derived due dates and are not marked as such** — nothing distinguishes them from entered ones, so a due date on an older nota may not be what anyone chose. They are correctable one at a time from the deliveries card.
- [ ] `updateDeliveryDatesAction` corrects an issued nota's dates with a required reason and an `AuditLog` row written in the same transaction. There is no UI that surfaces those audit rows next to the delivery — they are only visible through the general audit log viewer.
- [ ] The delivery dialog's `parseDateOnlyInput` duplicates `parseDateOnly`'s WIB-midnight parsing because the shared helper lives in a module the client can import but returns `undefined` rather than `null` and accepts looser input. Worth reconciling into one client-safe helper rather than two spellings of the same rule. Both were traced during the manual-nota-dates review: the client's accepted set is a strict SUBSET of the server's, so there is no behavioural divergence today — it is duplication, not a defect. The helper is now exported from `DeliveryFormDialog` and reused by the edit dialog, so there are two spellings, not three.
- [ ] Nota dates: a delivery retry after a lost response replays the `idempotencyKey` and returns the ORIGINAL delivery, discarding any dates the operator corrected between attempts, while reporting success. The key deliberately survives a failed submit so a genuine retry cannot double-ship, so the fix is a semantics decision — either detect a date mismatch on replay and return a distinct reason, or rotate the key whenever a date input changes — not a patch.
- [ ] Nota dates: the deliveries table shows the due date but never the invoice date, so after a correction an operator can verify only half of what they changed without reopening the dialog or printing. An eighth column would hurt the mobile layout; an `invoice → due` pair inside the existing column is the likelier answer.
- [ ] Nota dates: pressing Esc during a pending save, then opening the edit dialog on another row, lets the resolving transition's `setEditOpen(false)` snap the new dialog shut. Same pattern as the pre-existing create dialog, so fixing it means fixing both.
- [ ] `fmtDocDate`/`fmtDocDateTime` in `apps/web/lib/print/print-theme.ts` are now WIB-pinned (`timeZone: "Asia/Jakarta"`). They were formatting in the ambient zone, which is the BROWSER's — every print builder is called from a click handler in a `"use client"` file — so a WIB-midnight instant printed one calendar day early on any machine west of WIB. **On a WIB machine this changed nothing at all**; off WIB it corrected six other documents besides the nota tagihan (van load, van reconcile, konsi surat keluar, PO, material issue, PO payment receipt, plus the inventory and vendor-return timestamps). It would also have been wrong unconditionally had any of those ever been built server-side, since prod runs UTC. Remaining sibling of the same bug class: `toDisplayDate` in `lib/date-only.ts` (already listed under Sales UI / dates).
- [ ] `apps/web/app/actions/field-sales-deliveries.spec.ts` was named `.test.ts` when it landed, and `apps/web/vitest.config.ts` includes `lib/**/*.test.ts` but only `app/**/*.spec.ts` — so it had never executed, not even when named explicitly on the command line (a positional arg filters the include set, it does not widen it). Renamed to `.spec.ts` to match every other `app/` spec. **Anything under `app/` named `*.test.ts` is silently dead**; there is no guard against adding another one, and no CI job runs the suite at all.
- [ ] `updateDeliveryDatesAction`'s `CONFLICT` result is effectively unreachable in production. The `before` snapshot is read INSIDE the serializable transaction that then compare-and-swaps, and MySQL SERIALIZABLE promotes that read to a shared lock, so no writer can move the row in between — a competing correction blocks or deadlocks and is retried with a fresh read, after which the swap hits. The compare-and-swap is legitimate defence-in-depth and the audit `before` is now always truthful, but the operator-facing "someone else just changed this" toast will not fire in practice. The lost update an operator CAN still hit is the stale-UI one: B's dialog was seeded from server-rendered props showing X, A commits Y, B saves Z — B's transaction reads Y fresh, the swap hits, and Z wins silently with a truthful `before: Y`. Closing that would mean the dialog sending the pair it was seeded with as an optimistic token.
- [ ] The `CONFLICT` spec case drives a mocked transaction client that writes outside a real transaction, so it proves no audit row is written on a compare-and-swap miss but does not prove transactional rollback. Rollback was confirmed structurally instead — both early returns precede the audit create inside the same callback. The test is not weaker than it claims, just narrower than it looks.
- [ ] `apps/web/lib/print/print-theme.ts` is now mixed-quote: the two timezone-fixed formatters use double quotes per the house rule, while `esc` and `money` beside them still use single. Consistent where touched, inconsistent within the file.
- [ ] Faktur pajak: `recordNotaTagihanPrinted` is best-effort end to end — a failed audit write, a failed notification create or a rejected fan-out is caught and `console.error`'d, and the action returns `void` either way, so the operator is never told and no durable flag records it. A nota can therefore print with finance never notified and nothing anywhere saying so; the only recovery is that the row is already in the queue by construction (it is created with the delivery, not by the print), which is why the queue rather than the ping is the durable signal. Same shape as the van journal actions' silent success, and it would want the same fix: return the failure and warn.
- [ ] Faktur pajak: `TaxInvoice.markedById` and `notaPrintedById` carry no `@@index`, unlike `status`. Nothing queries by them today — the queue filters on status and searches through the delivery — so this only bites the first "what did this user mark" report, or a `User` delete that has to scan for references under `relationMode = "prisma"`.
- [ ] Faktur pajak: the print click blocks on FIVE sequential server round trips (auth, the print audit write, the CAS, the delivery lookup, the notification create) before `printHtml` opens the preview, where it used to await one `logPrint`. The button is disabled through the transition so a double-click can't double-fire, but the operator waits on work that has nothing to do with the document being rendered. The notification half could be fired and forgotten after the CAS, or the whole call moved after the preview opens.
- [ ] Faktur pajak: the nota tagihan print dialog's confirm button is disabled on `isPending` but keeps its normal label, unlike every sibling dialog on the same card (close-remainder, update-dates) and the faktur queue's own dialogs, which all swap to a "submitting…" label. With five round trips behind it (item above) that silent gap is the longest on the page.

### Sales UI / dates
- [ ] Adopt `parseDateOnlyEnd` (WIB-safe inclusive `to`) on the remaining date-filtered lists: returns, fulfillment, van-sales, supplier-payments, reports/hpp, stock-card (only journals/ledger/sales-orders use it today) (PR #142).
- [ ] `toDisplayDate` in `apps/web/lib/date-only.ts` calls `toLocaleDateString("id-ID")` with no `timeZone`, so it's process-local; prod runs UTC. Same bug class as the report date labels fixed in PR #215, which is why `formatDateOnlyJakarta` now exists — its callers need auditing.

### Notifications
- [x] The three web `AdminNotification` categories now reach the in-app bell. `fanOutAdminNotification` (`lib/notifications/admin-fanout.ts`) runs after each flag write and fans out `NotificationQueue` rows to the users holding the permission that can ACT on the category — `journals:manage`, `field_sales_orders:approve`, `stores:manage` — reusing the same delivery helpers the four existing push types use. Best-effort by design: a failure costs a bell ping, not state.
- [ ] **`AdminNotification` is NOT write-only, and must not be deleted.** It is a durable per-document state flag, and `lib/canvassing/journal-pending.ts` reads it both to gate the "Post journal" retry button on four screens AND to authorize `postVanLoadJournalAction` / `postVanSaleJournalAction` / `postVanReconcileJournalAction` — every export of a `"use server"` module is an independently callable endpoint. `NotificationQueue` is per-user delivery; conflating the two would make a retry button's authorization depend on whether somebody read their notification. The eventual clean split is to move the journal-pending flag to a purpose-built column or table, after which the model really can go.
- [ ] `apps/api`'s `AdminNotificationService.write` still writes flag rows that reach nobody. The api has no Firebase admin and `NotificationQueue` delivery is web-owned, so wiring it needs a cross-service decision.
- [ ] `app/actions/notifications.ts` is `'use server'` with ~18 exports and no `auth()` or `hasPermission` check in the file. No client component imports it, so the action ids do not ship in a browser bundle and this is a hardening gap rather than a live hole — but Next registers every export as a callable action. The two delivery helpers were moved to `lib/notifications/recipients.ts` to get them out of the registry; the rest were left alone.
- [x] Prod RBAC precondition for the fan-out VERIFIED 2026-08-13: `journals:manage`, `field_sales_orders:approve` and `stores:manage` all exist as `Permission` rows on prod, so all three categories resolve recipients normally. Prod resolves **1 recipient** for every category — a single ADMIN user, via the `isSystem` branch. Note dev is MISSING `journals:manage` (only `packages/db/prisma/seed.ts` defines it and the deploy pipeline never seeds), which is what the `getUsersWithPermission` missing-row fix covers — dev still resolved its admin.
- [ ] **Backfilling the `JOURNAL_PENDING` backlog is measured and DEFERRED — it is NOT a routine post-merge step.** The script is `pnpm exec tsx scripts/backfill-journal-pending-notifications.ts` from `apps/web` (dry-run first, then `--apply --confirm-prod-writes`); idempotent, dedups on the `AdminNotification` row id. Dry-run against prod 2026-08-13: it would create exactly **50 rows for 1 recipient**. All 50 targets were written in a single 4-second window on `2026-07-26T04:00:01–04:00:03`, every one `"Sales journal not posted"` with `reason: UNMAPPED_ROLE` (`AR`/`SALES_REVENUE`/`COGS`) — one sweep-stall batch, not 50 distinct incidents. That falsifies the spec's D6 rationale ("these stay actionable indefinitely, someone must press the retry button"): nobody can act on them while the GL cutover stays unarmed, and once it IS armed `postPendingSalesJournals` re-posts them with no button pressed, so the flags go stale on their own. Applying it today takes the bell from 7 rows to 57 — past the hard `LIMIT = 50` — evicting all 7 real notifications, moving the badge from 5 to 55, with no mark-all-read for the single admin to clear it and no document number in any of the 50 otherwise-identical messages. Revisit after mark-all-read ships, or drop it.
- [ ] Bell items for the three newly-delivered categories navigate nowhere. `getNotificationHref` (`lib/notifications/navigation.ts`) has cases for ~18 existing types but none for `JOURNAL_PENDING`, `PENDING_ORDER_APPROVAL` or `STORE_CHANGE_REQUEST`, and returns `null` for anything unmapped — so those items render and mark-as-read on click, but do not link through. `PENDING_ORDER_APPROVAL` (`orderId`) and `STORE_CHANGE_REQUEST` (`storeId`) are one-line cases; `JOURNAL_PENDING` is genuinely varied (`docId`+`kind`, `orderId`+`kind`, `opnameId`, `salesReturnId`, `receiptId`+`woId`) and needs a per-kind mapping. Note the one-shot backfill wrote `data` containing only `adminNotificationId`, so backfilled rows would lack any metadata field a future case relies on.
- [ ] **The bell can permanently fill up, and on prod it will.** `app/api/notifications/route.ts` has a hard `LIMIT = 50` with no cursor and no load-more in `NotificationIcon`; the only way to set `readAt` is clicking an individual item (no mark-all-read); `unreadCount` counts **all** unread rather than the visible 50; and nothing anywhere prunes `NotificationQueue`. So once an admin passes 50 unread rows, everything past position 50 is unreachable from any surface and the badge can never return to zero. The ceiling is real and unfixed, but the URGENCY was measured on prod 2026-08-13 and is far lower than this item used to claim. Prod has **1 admin, not three**; `JOURNAL_PENDING` is **0/day** over the last 7 days (50 total, all one batch on 2026-07-26); `PENDING_ORDER_APPROVAL` is **1 row ever** (2026-07-06); `STORE_CHANGE_REQUEST` is 0; and `NotificationQueue` holds 7 rows. So the live fan-out is not filling anything today, and the van-sale flood this item used to predict is contingent on canvassing entering real prod use while `CASH` is still unmapped — which has not happened. **The superseded estimate ("twenty sales a day across three admins, sixty rows a day") was extrapolated from the dev bed, which is the opposite shape** — dev runs 22.5/day `PENDING_ORDER_APPROVAL` and sits at 45 unread of 52, because it absorbs all the field-sales testing. Never size prod notification volume from `:3308`. Cheapest useful pair remains a mark-all-read endpoint plus either pagination or a retention window; mark-all-read is the one that unblocks the deferred backfill above.
- [ ] Several `JOURNAL_PENDING` messages name no document. `app/actions/grn.ts` says "post from the GRN row" with no `docNumber` anywhere in the title, message or metadata — only `grnId`. Same for FG receipt, sales return and supplier payment. Before the fan-out nobody read those strings; now they are the entire user-facing surface, so a bell item saying "a GRN journal failed" with no document number and no link is delivered but not actionable. Fixing it means changing the `AdminNotification` title/message at four sites, which the fan-out branch deliberately forbade itself from touching.
- [ ] Best-effort delivery is safe "because another surface still tells someone" for six of the eight `JOURNAL_PENDING` sources and for both other categories, but that argument **fails for the supplier-payment `payment` direction**: its retry hint is "unmark payment on the PO and mark it paid again", and a PO left marked paid with no payment journal is already recorded above as invisible after a page reload. The bell is that alert's first and only rendering, so its delivery is genuinely load-bearing rather than redundant.
- [ ] A fourth `AdminNotification` category exists and stays bell-invisible: `STOCK_OVERSELL_RISK`, written from `packages/db/src/reservation-writer.ts` on both the Jubelio reserve path and the field-sales reserve path. Out of scope for the same reason as the api-side writer — `packages/db` has no Firebase and wiring it needs the cross-service decision — but the fan-out spec says "three categories", which reads as full web coverage.
- [ ] The backfill's documented apply command passes `--confirm-prod-writes`, which is the single flag that disables every branch of its port guard. Following the documentation degrades the prod check to a `console.warn`; the only real gate is `--apply`.
- [ ] The backfill selects on `readAt: null`, but nothing in the codebase ever sets `AdminNotification.readAt`, so that filter is a no-op and its scope is every `JOURNAL_PENDING` row ever written — no date floor, no cap. Its dry run does print the counts before any write.
- [ ] Running the backfill while the live path is also writing can duplicate a bell row for the same alert: the backlog read and the already-delivered read are not point-in-time with each other. Run it during a quiet window.
- [ ] `getUsersWithPermission`'s missing-permission-row branch (`lib/notifications/recipients.ts`) has no test. It is the fix that makes the fan-out reach anyone on prod at all — without it a missing `Permission` row returned `[]` and silently excluded even system-role admins, who hold the `'*'` wildcard from `lib/auth.ts`. There is no `recipients.test.ts`, and `admin-fanout.test.ts` mocks `./recipients` wholesale, so nothing exercises it: someone who later "simplifies" the ternary back to an early return would see every spec still pass while the behaviour silently regresses. It IS testable — `getUsersWithPermission` is not behind the `VITEST` guard, so a DB spec could seed an `isSystem` role and call it with a permission code that has no row. That was deliberately not added here, because seeding roles and users against the shared `:3308` bed is the same hazard class this branch spent two fix rounds removing from other specs, and it would need teardown through `seededId()` to be safe.
- [ ] `getNotificationHref` (`lib/notifications/navigation.ts`) has zero test coverage — the whole file, not just the `FIELD_RETURN_MISMATCH` case added alongside it. It's a pure function of `(type, data)` with no database, so a first spec is cheap, and it now maps roughly twenty categories: a wrong or transposed case is invisible until someone clicks that specific bell item and lands on the wrong page (or nowhere, via the `null` fallback) — a wrong-link failure, not data corruption, which is why this was judged non-blocking rather than fixed inline (feat/field-retur-mismatch-notification).
- [ ] The two `FIELD_RETURN_MISMATCH` cases in `receive-writer.test.ts` prove the `AdminNotification` row's content but never that `fanOutAdminNotification` is actually called with it — the spec mocks `@/lib/notifications/admin-fanout` wholesale (same pattern `field-sales/writer.test.ts` uses), and the real function additionally suppresses delivery under `VITEST` regardless. A regression that wrote the row and simply forgot the `void fanOutAdminNotification(notification)` call would leave both new tests green while delivering the notification to nobody — the same silent-total-failure shape as an unmapped category. Today this is proven only indirectly, via `admin-fanout.test.ts`'s recipient-resolution assertions for the category, never end-to-end from the writer (feat/field-retur-mismatch-notification).
- [ ] A stale-preview race in the retur approve dialog: cancelling and reopening mid-fetch can let a stale response overwrite a fresh one, because `setStockImpact` has no request-id guard. Display-only; the writer stays authoritative (feat/konsi-virtual-warehouse).
- [ ] The `KONSI → PUTUS` flip guard (`apps/web/lib/stores/queries.ts` `updateStore`) is a plain pre-check, not transaction-isolated against a konsi transfer landing between the check and the store update (feat/konsi-virtual-warehouse).

### Ops / infra
- [ ] `packages/db/prisma/migrations/20260813120000_add_tax_invoice/migration.sql:18` points a reader at `CLAUDE.md` for text that now lives in `docs/ARCHITECTURE-NOTES.md`, and **it must not be corrected**: Prisma checksums every applied migration into `_prisma_migrations`, so editing the file — even a comment — makes `migrate deploy` fail with a checksum mismatch against prod, where this migration is already applied. A stale doc pointer is a papercut; a repo that cannot deploy is an outage. The same rule applies to EVERY already-applied migration file, so never sweep migrations for doc-pointer rot. If one ever genuinely needs correcting, the route is a new migration or a `prisma migrate resolve`, not an edit in place.
- [x] DB backups — LIVE on prod since 2026-08-08. `scripts/backup-db.sh` installed on the VPS, cron 19:15 UTC (02:15 WIB), writing to the private `elorae-backups` R2 bucket. First run verified end-to-end: 91,471,524 bytes, decrypt-verified, upload size-confirmed, and the daily + monthly objects both exist. **Decryption with the off-site passphrase separately proven** — the password-manager copy (not the server's file) was tested against the live archive and decrypts it to a complete dump. AWS CLI is user-local at `~/.local/bin` because `awscli` is not apt-installable on this host, so the crontab MUST set `PATH` (PR #227).
- [ ] Nothing has yet proven the CRON invocation works — every successful run so far was manual. A cron-only failure (PATH, environment, working directory) would surface only in `/home/elorae/backup.log`. Check it once, then this is genuinely closed.
- [ ] No restore DRILL has been done. Decryption is proven; actually loading a dump into a scratch database and looking at it is not, and a dump can decrypt perfectly while failing to import (charset, a table the schema no longer has, an interrupted section that still ends with the trailer). The README documents the drill — run it once against a real archive, then again whenever the schema changes shape.
- [x] `--copy-props none` is required for any bucket-to-bucket `aws s3 cp` on R2: the default carries object tags across via `GetObjectTagging`, which R2 does not implement, so the copy fails `NotImplemented` while the source object is perfectly fine. Found on the first live backup run; the same trap awaits any future R2 copy code (PR #227).
- [ ] Backup failure is visible only in `backup.log` on the VPS — nothing renders it and nothing alerts. Same gap as the `AdminNotification` feed; a health-check ping or a "last successful backup" surface would close it.
- [ ] **GitHub Actions cache hits the hard 10 GB per-repo ceiling in about two days of pushes** — measured 2026-08-09 at 9.996 GiB across 123 entries, of which 9.35 GB had not been READ since 6–7 August while that day's runs touched only 91 MB. At the cap, every run evicts LRU entries, so the Docker layer cache thrashes rather than helping. Cause: `deploy.yml` exported with `cache-to: type=gha,mode=max` on both `scope=web` and `scope=api`, which writes every intermediate layer (pnpm store, `node_modules`, `.next`, Prisma engines) — largest single blobs 738 MB / 513 MB / 441 MB. Switched to `mode=min` (PR #230) and the stale entries were purged by hand; GitHub's own 7-day unused-eviction would not have cleared them until ~13–14 August. **Re-check `gh api /repos/.../actions/cache/usage` periodically** — if it climbs back to the cap, the next lever is dropping `scope=api` caching entirely, since the api image is the smaller of the two.
- [ ] **A wedged SSH tunnel presents as a Prisma pool timeout, not a network error** — cost 30 min on 2026-08-13. `ssh -fNL` opens the LOCAL listener immediately and keeps it open even after the master connection to the VPS goes deaf, so the port looks healthy in `ss -ltn`, raw TCP connects fine, and the only symptom is `pool timeout ... (pool connections: active=0 idle=0 limit=10)` — which reads like an app or connection-leak bug. **`active=0` is the tell: the socket never opened at all**, so it is never a leak. Confirm with `ss -tnp | grep <ssh-pid>` — a wedged tunnel shows the outbound `:22` connection `ESTAB` with a stuck non-zero **Send-Q** (data queued, never acked) and a pile of local `CLOSE-WAIT` entries behind it; a `mysql` client against the tunnel gives `ERROR 2013 ... waiting for initial communication packet, system error: 110`. Remedy is `kill <pid>` and reopen. **Always open the tunnel with `-o ServerAliveInterval=30 -o ServerAliveCountMax=3 -o ExitOnForwardFailure=yes`** so it tears itself down within ~90s of going deaf instead of leaving a zombie listener, and fails loudly at startup rather than listening-but-not-forwarding. Same `active=0` signature also appears when nothing is listening at all (the `:3308` dev bed with Docker down) — check the listener before blaming the tunnel.
- [ ] Auto-schedule the R2 orphan sweeper (not registered in `lib/cron/jobs.ts`) (PR #86).
- [ ] Queue observability: depth alert (AdminNotificationService) + dashboard panel; sweeper DEAD-letters after N attempts; prod-mode singleton smoke test (none in apps/api) (PR #73).
- [ ] VPS log shipping (Loki/Logtail) for off-server retention + backup Redis + Caddy cert volumes (PR #53).
- [ ] Trim web image size — drop unused Prisma engine binaries from the runtime layer (PR #56).
- [ ] Catalog sync: re-introduce the dropped progress log via an atomic counter (`catalog-sync.service.ts` logs only an end summary) (PR #39).

### Testing / infra
- [x] `packages/db` spec teardowns could truncate seven tables — every filter in all four DB-touching specs now goes through `seededId()` (`packages/db/src/spec-teardown.ts`), ids are declared `= ""` and reset at hook top, and `journal-writer.spec.ts` (found during review, one throw from wiping the whole `Journal` ledger) got the same treatment. Mutation-proven for the reservation-writer and backfill specs (PR #223).
- [ ] **SAME HAZARD STILL LIVE IN `apps/web` — the larger half.** 17–18 test files declare `let <x>Id: string;` and filter teardowns on it against the same `:3308` bed (e.g. `lib/finance/journal.test.ts:13-15`, `lib/purchasing/set-po-paid-at-action.test.ts:45-49`, `lib/field-sales/approve-action.test.ts:50-57`, `lib/field-sales/queries.test.ts:76-87`, `lib/finance/journals/mapping.test.ts:40-41` — that last one can delete the whole `ChartAccount` table). Plus two that over-match with NO variable ever being undefined: `app/pwa/stores/actions.spec.ts:64` deletes every store with a `TEST-`-prefixed code, and `lib/store-changes/writer.test.ts:32` deletes every `STORE_CHANGE_REQUEST` notification created since the test started — including ones a running dev app wrote. `packages/db` is done; do not read the class as closed (PR #223).
- [x] A lint rule flags **bare-variable id filters** (shorthand `{ itemId }` AND explicit `{ id: leafId }` alike) inside `deleteMany`/`updateMany` in `apps/web` spec/test files — `no-restricted-syntax` in `apps/web/eslint.config.mjs`. Passing the id through a guard call satisfies it. **Covers the undefined-collapse hazard only.** It does NOT catch a teardown that over-matches real rows with no variable involved (`{ code: { startsWith: "TEST-" } }`, a bare `createdAt` range) — those still need review judgement, and two live examples are listed above (PR #224).
- [ ] That rule is **WARN, not error**, because ~18 existing `apps/web` test files still violate it and a red `eslint .` helps nobody. **Flip it to `"error"` the moment that sweep lands** — a rule nobody must obey is decoration.
- [ ] The rule does not cover `packages/db`, which has no ESLint config and no eslint devDep (adding one means a new dependency + install). `packages/db` is clean today, so this is regression cover it lacks, not a live defect.
- [ ] Neither CI workflow runs lint, tests or type-check — only `bugbit-pr-review.yml` and `deploy.yml` exist. So every check in this repo is opt-in on a developer's laptop, and the lint rule above only fires for someone who chooses to run it. A CI job is what makes any of it enforcement rather than advice.
- [ ] Resetting fixture ids at hook top (PR #223) removed master's accidental self-healing: ids used to carry over, so a partially-failed teardown got a second attempt on the next test. Now a partial teardown leaks permanently and, where the leaked row collides with a describe-scoped unique code, every remaining test in the file fails `P2002`. Correct trade — a leak beats a truncation — but it is why `:3308` currently holds 4 orphan `TEST-UOM-TEST-FS-*` rows with no matching Item, from four separate past runs. Worth a one-off cleanup.
- [ ] `apps/web/lib/finance/journals/mapping-test-fixture.ts:26-36` — `restoreMappings` loops the four roles unguarded, so a throw on the first leaves the rest of the live GL config pointing at throwaway test accounts. This is the fixture the hardened specs depend on for their "restore shared config first" step, so the one step that must never be skipped can still half-fail (found by PR #222's whole-branch review).
- [ ] Sales-sweep specs write the cron's own arming switch (`SystemSetting["finance.glCutoverDate"]`) onto the shared bed. Teardown restores it first and is step-isolated, but a run KILLED between `beforeEach` and teardown leaves `:3308` armed at the test value — and dev has all four sales roles mapped with 205 eligible orders, so the dev cron would then post ~410 journals, after which the next run snapshots the leaked value as the real config. Only closed properly by DB-spec isolation (PR #222).
- [ ] DB-spec isolation: `vitest.config.ts` `fileParallelism:false` serializes the WHOLE suite (slow). Scope DB specs into a serial project, or add a tx-rollback harness for the ~20 real-row specs.
- [ ] `apps/web/lib/finance/settlement/journal.test.ts`'s `afterEach` is order-fragile: a failure partway through leaves stale ids so a delete throws and aborts the hook before later cleanup, leaking a row into the shared dev database.
- [ ] DB specs mutate the operator's real `JournalAccountMapping` on the shared `:3308` bed; a `beforeAll` that exceeds its timeout leaves a zombie hook that can re-point live GL config after `afterAll` has already restored it. Mitigated (single `beforeAll` window, `afterAll` net, loud restore failure) but only closed properly by DB-spec isolation — a dedicated schema or a tx-rollback harness. Note it caused a real incident on 2026-08-05.
- [ ] `apps/web/lib/finance/settlement/journal.test.ts` still seeds 8 chart accounts and 8 mappings in `beforeEach` with an unguarded `afterEach`, the same shape as the fixture that failed — give it the `beforeAll` + guarded-`afterAll` treatment next time that file is opened.
- [x] `supplier-payment-journal.test.ts` reused `docNumber` suffixes across cases on `@unique` columns, so a partial teardown surfaced as a unique-constraint failure in a LATER, unrelated test — all 63 literals now come from a describe-scoped counter, so the collision can't be hand-reintroduced (PR #220).
- [ ] Same hazard one table over in that file: the alternate `chartAccount.code` literals (`9${token}9/7/6`) are still hand-numbered on a `@unique` column (PR #220).
- [ ] `apps/web/lib/field-sales/writer.test.ts` and `apps/web/lib/field-sales/approve-action.test.ts` still carry ~20 bare-variable teardown filters with no `seededId()` — the same undefined-collapse hazard the lint rule warns about. The delivery specs (`delivery/writer.test.ts`, `delivery/queries.test.ts`, `reservation-writer.delivery.spec.ts`) are clean; these two were not swept (feat/field-sales-delivery).
