# EPIC decomposition + status

Per-EPIC sub-project tables: what each slice covers, what shipped, and which PR shipped it. Read it before building anything that sounds like it might already exist, or when you need to know what a shipped slice actually included. Split out of `CLAUDE.md` on 2026-08-25; the text is unchanged, and the maintenance rule near the bottom of this file still applies — tick the row here in the same session a slice merges.

EPIC-01 (Jubelio Integration) + EPIC-02 (Product & Stock Sync) are decomposed into 5 sub-projects, each its own spec → plan → PR cycle:

| Sub | Scope | Status | EPIC stories |
|----|-------|--------|--------------|
| **1** | Inbound webhook queue + stock handler | ✅ shipped (PR #29 merged 2026-05-28) | 01-02 (partial — stock only), 01-03 (inbound half), 02-04 |
| **2** | Outbound `JubelioOutbox` + first push primitive | ✅ shipped (product + stock handlers, outbox router/poller/processor) | 01-03 (outbound), 02-03 |
| **3** | Product push + HPP/price sync | ✅ shipped — product push (PRs #37/#42) + HPP→sellingPrice auto-recalc with audit log (PR #52 merged 2026-06-14). `buy_price` intentionally stays global per decision H3; revisit if Jubelio's buy_price column starts feeding downstream marketplace reporting. | 02-01, 02-02 |
| **4** | Remaining inbound handlers (salesorder, salesreturn, product webhooks) | ✅ shipped — salesorder + product handlers (PR #40); **salesreturn inbound wired via EPIC-05 (PRs #54/#55)** — returns ARE SalesOrders, handled by `SalesOrderWebhookHandler` (the old "stub awaiting samples" note is stale). Only the *outbound* `salesreturn_decision_push` remains unwired — see Follow-ups. | 01-02 (full) |
| **5** | Initial bulk migration tool | ✅ shipped (PR #41 merged 2026-06-10) | 02-05 |

EPIC-03 (Sales — Orders) status:

| Story | Scope | Status |
|-------|-------|--------|
| 03-01 | Order ingestion via Jubelio salesorder webhook | ✅ shipped (PR #43 merged 2026-06-11) |
| 03-02 | Order dashboard (list + filters) | ✅ shipped (PR #44 merged 2026-06-11) |
| 03-03 | Order detail view | ✅ shipped (PR #44) |
| 03-04 | KPI widgets on beranda | ✅ shipped (PR #45 merged 2026-06-11) |

EPIC-04 (Sales Fulfillment) decomposition (independent of EPIC-01/02 sub-numbering):

| Sub | Scope | Status |
|----|-------|--------|
| **A** | Fulfillment backend (writer helper, server actions, status transitions, outbox enqueue on pick/pack/ship) | ✅ shipped (PR #47) |
| **B** | UI actions on order detail page + JubelioCourier sync | ✅ shipped (PR #48) |
| **C** | Fulfillment Queue page + Jubelio webhook forward-sync to `SHIPPED` | ✅ shipped (PR #49) |
| **D** | Print views (pick list + packing slip) | ✅ shipped (PR #50 merged 2026-06-14) |

EPIC-05 (Sales Returns) decomposition:

| Sub | Scope | Status |
|----|-------|--------|
| **A** | Schema + helpers + Jubelio client methods + ingest service + webhook handler + 30-min backstop sweeper | ✅ shipped (PR #54 merged 2026-06-18) + hotfix PR #55 reorienting the ingest around Jubelio's actual data model (returns ARE SalesOrders; `SalesOrderWebhookHandler` is the authoritative entry point; URL/field corrections). Carries 2 items into sub-B: race-condition serialization on concurrent Accept, variant-SKU fallback lookup in ingest. |
| **B** | Server actions + per-item Accept/Reject UI on detail page (outbox decision-push handler deferred to sub-B.5 pending Jubelio resolve-endpoint docs) | ✅ shipped (PR #58 merged 2026-06-19) — includes Redis-lock serialization on concurrent Accept from sub-A carryover |
| **C** | Dashboard list + KPIs + RBAC seed + i18n | ✅ shipped (PR #58 merged 2026-06-19) — bundled with sub-B. Follow-ups: totalQty Decimal coercion hotfix (PR #59), back-to-list nav polish |

EPIC-08 (Reserved Stock) decomposition:

| Sub | Scope | Status |
|----|-------|--------|
| **A** | `StockReservation` ledger + `InventoryValue.reservedQty` aggregate + `reserveOrder`/`consumeOrder`/`releaseOrder` helpers + `FULFILLMENT_CONSUME` source + Jubelio stock push sends `available` | ✅ shipped (PR #88 merged 2026-07-02). **Post-merge deploy pending:** run `prisma/backfill-reservations.ts --apply` against prod (webhook-quiet window) to reconcile existing orders — see `docs/local-db-testbed.md`. |

EPIC-17 (Field Sales / SFA) decomposition:

| Sub | Scope | Status |
|----|-------|--------|
| **1** | PWA scaffold — `SALESMAN` role + `pwa:access` permission + `/pwa/*` route tree + Serwist SW scoped `/pwa/` + post-login redirect + salesman seed user | ✅ shipped (PR #90 merged 2026-07-02) + hotfixes PR #91 (relative Location on login redirect for reverse proxy + server-side redirect + drop theme owner-gate + SALESMAN `isSystem=false` so it doesn't inherit wildcard perms) and PR #92 (webpack builder — Turbopack doesn't run Serwist plugin). Root next-pwa on `/` coexists untouched. |
| **2** | Store master (17-08) + Check-in/out with mandatory GPS (17-01) — `Store` + `StoreVisit` + `StoreTermsType` schema, `stores:view`/`stores:manage` permissions (admin wildcard covers; DB rows seeded post-merge), backoffice CRUD (URL-driven pagination + Card-wrapped list + sidebar-layout detail with visit history + sectioned form), PWA home with nearest-3 + `/pwa/stores` list sorted by GPS distance + `/pwa/stores/[id]` detail with mandatory-GPS check-in/out, one-active-visit-per-user auto-close (Serializable isolation + atomic checkout via `updateMany where checkoutAt is null`). Server actions return `{ok:false; code}` (not throw) — prod digest-masking safe. Dev SW registration guarded (Serwist disabled in dev). | ✅ shipped (PR #94 merged 2026-07-03). Post-merge: seed run against prod tunnel adds `stores:view`/`stores:manage` Permission rows (57 total). |
| **3** | Product catalog view + real-time stock + per-store pricing + offline read-only (17-02) — `computeStorePrice` pure helper in `@elorae/db/pricing` client-safe subpath (PUTUS passthrough; KONSI gross-up `sellingPrice/(1-margin%/100)` shown as informational retail — reused by sub-4/5 order totals), `listCatalogForPwa` query/serializer (active `FINISHED_GOOD` only, `available = qtyOnHand - reservedQty` via `aggregateInventoryValues`, batched primary image), `GET /pwa/api/catalog` route (auth + `pwaAccessGuard`), `CatalogShell` client (client-side fetch so SW can cache, in-memory SKU/name search + category filter, local pagination 10/page compact icon pager, back-to-store nav, segment `loading.tsx`, compact one-row cards). SW `StaleWhileRevalidate` on the route = offline read-only catalog. **Fetch-all-per-store is deliberate** (offline needs full set client-side; lean ~1 kB/item metadata, images lazy); tripwire ~800–1000 active FG → switch to store-independent fetch + on-device pricing via the same helper. **Offline WRITE queue for orders** shipped separately in the **Offline** row below (was deferred here) — sub-3 itself is read-only. Shadcn `Card` gotcha: defaults to `flex flex-col`, so horizontal rows need explicit `flex-row`. | ✅ shipped (PR #95 merged 2026-07-03). Follow-ups (non-blocking): i18n wiring (catalog strings hardcoded ID), inactive-store deep link shows as load error, serializer Decimal-path + route-handler test coverage. |
| **4a** | Taking Order (Putus) backend (17-03) — `FieldSalesOrder`/`FieldSalesOrderLine` (web-owned, ERP-only; `SalesOrder` untouched), lifecycle create→approve→reject in `apps/web/lib/field-sales/writer.ts`, min-qty (`SystemSetting putus.minOrderQty=6` + nullable `Item.minOrderQty` override), reserve-at-create/consume-at-approve/release-on-reject via NEW standalone `reserveFieldSalesOrder`/`consumeFieldSalesOrder`/`releaseFieldSalesOrder` in `@elorae/db/reservation-writer.ts` (Jubelio trio untouched), `StockReservation` gained `source` (`JUBELIO`\|`FIELD_SALES`) + nullable Jubelio Ints + `fieldSalesLineId`, approve materializes `SalesHistory` (`channel=OFFLINE`, COMPLETED — feeds forecasting per D2), `PENDING_ORDER_APPROVAL` AdminNotification, `PUTUS` added to `DocType` enum (orderNo via `generateDocNumber`). Item-level not per-variant (see BOUNDARY D15). | ✅ shipped (PR #98 merged 2026-07-04). Variantless inventory uses `variantSku: null`; field-sales reserve lookup tolerates null-or-empty (real-data bug fix). |
| **4b** | Taking Order (Putus) PWA UI (17-03) — cart-on-catalog (qty steppers gated on active check-in, sticky total bar), in-shell review view (editable lines + note + Kirim), online-only submit via `submitFieldSalesOrder` action → `createFieldSalesOrder`; catalog payload gained `itemId`; "Harga belum diset" tag for in-stock-unpriced items; multi-item min-qty error; ResizeObserver-measured sticky-bar spacers. Home active-visit card gained a "Katalog Produk" CTA. | ✅ shipped (PR #101 merged 2026-07-04). Follow-ups: ~~server-side `unitPrice` recompute~~ ✅ done (#207, putus now server-priced); shared `QtyStepper`; CatalogShell i18n retrofit. |
| **4c** | Taking Order (Putus) backoffice approval (17-03) — `field_sales_orders:view`/`approve` permissions (admin wildcard; seeded), `listFieldSalesOrders`/`getFieldSalesOrderById` queries, `approveFieldSalesOrderAction`/`rejectFieldSalesOrderAction` (permission-gated, wrap 4a writers), backoffice list (mirrors sales-orders) + detail + `ApproveRejectCard` (AlertDialog confirm, required reject reason) under the Sales-Orders nav group, i18n (43 keys id/en). | ✅ shipped (PR #102 merged 2026-07-04). **Post-merge: re-seed `field_sales_orders` permissions on prod** (deploy pipeline doesn't seed). Follow-ups: shared format helpers, cross-page nav pending-state, CI `tsc --noEmit` gate. |
| **5a** | Taking Order (Konsi) backend (17-04) — `orderType` (PUTUS\|KONSI) discriminator on `FieldSalesOrder` (derived server-side from `Store.termsType`), `ReserveSource.FIELD_SALES_KONSI` + `DocType.KONSI`, `reserveKonsiFieldSalesOrder` (atomic guarded `UPDATE … WHERE qtyOnHand-reservedQty>=qty` → block-on-short into `shortLines`, never consumes), `createFieldSalesOrder`/`approveFieldSalesOrder` konsi branch (no min-qty, reserve-at-approve, gross-up snapshot via `computeStorePrice`, **NO `SalesHistory`** — transfer not sale, `InsufficientStockError` on short), deadlock-retry helper `withRetry`/`runSerializable` (`lib/db/tx-retry.ts`, retries whole SER tx on MySQL 1213/P2034), queries (`orderType` filter, `sentItemIds` never-sent helper, per-line `available`). Konsi is capture+approve only; real stock transfer to store virtual warehouse deferred to EPIC-18-04/19. | ✅ shipped (PR #105 merged 2026-07-05). |
| **5b** | Taking Order (Konsi) PWA UI (17-04) — `CatalogShell` KONSI mode (branch on `termsType`; hides ALL pricing incl. off-wire — konsi catalog payload nulls `price`/`priceLabel` server-side; "Baru" badge on never-sent; "Sarankan produk baru" auto-suggest, global self-disabling + added-count toast; qty+SKU-count sticky bar), catalog payload gains per-item `neverSent`. Submit unchanged (server derives orderType, konsi lines carry price 0). | ✅ shipped (PR #107 merged 2026-07-05). |
| **5c** | Taking Order (Konsi) backoffice approval (17-04) — reuses the field-sales-orders dashboard branched on `orderType`: order-type filter + Type column/badge, konsi detail ("Transfer Konsinyasi", always-shown Availability, price/subtotal/total only when `!isKonsi \|\| APPROVED`, margin-invalid/unset warning), `ApproveRejectCard` konsi confirm copy + `INSUFFICIENT_STOCK` toast, i18n (11 keys id/en). No new route/nav/permission (reuses `field_sales_orders:*`). | ✅ shipped (PR #108 merged 2026-07-05). Dormant until a `termsType=KONSI` store exists. |
| **6** | Promo/scheme discounts on PUTUS orders (17-06) — ONE PR (feature atomic). `Promo`/`PromoItem`/`PromoStore`/`PromoTier` + order discount fields; pure `@elorae/db/promo` `computeOrderPromos` engine (PERCENT/FIXED-per-unit/TIERED; best-value line-promo per line + one order-promo on **net** subtotal; tiebreak priority-then-id; below-cost flag); putus create applies **server-authoritatively, honor-at-create** (client never sends discounts; approve honors persisted discount, no recompute) → net `total` + net `SalesHistory`; backoffice `/backoffice/promos` CRUD + `promos:view`/`promos:manage`; field-sales-order detail promo display + below-cost warning; PWA read-only preview + review discounted quote; i18n both locales. **Putus only** (konsi shows salesman no price). Bonus-qty explicitly out. | ✅ shipped (PR #111 merged 2026-07-05). **Post-merge: `promos:*` permissions seeded on prod** (2 Permission rows + ADMIN link, surgical SQL). Dormant until an admin creates a promo. Follow-ups: order-discount not pro-rated across SalesHistory lines (D7 — orderTotal authoritative); preview debounce; zero-promo short-circuit. |
| **Offline** | PWA offline order queue + offline navigation — closes the offline-WRITE debt deferred out of sub-3/4/5. Dexie pending-order queue (`apps/web/lib/pwa/offline/`); client-UUID `idempotencyKey` end-to-end (`FieldSalesOrder.idempotencyKey @unique` + writer short-circuit) so replays dedup server-side (DB constraint is the real guarantee); app-level sync (`setupOrderSync`: `online` + `visibilitychange` triggers, run-locked, reclaims orphaned `syncing`); pure terminal-vs-transient classifier (`classify.ts` — type-only `SubmitResult` import, no next-auth chain); `/pwa/orders/pending` view; **visit-binding** — captured `visitId` threaded through submit → `createFieldSalesOrder` (validates visit belongs to salesman+store, no open-check) so orders still sync after checkout. Serwist SW (`app/pwa/sw.ts`): `NetworkFirst` for `/pwa` document navigations + RSC into `pwa-pages` (3s timeout, ExpirationPlugin 50/7d), install-precached static-free `/pwa/offline` fallback (`force-dynamic`, non-fatal precache), `handlerDidError` → offline page; catalog SWR entry stays first, matchers ordered RSC→navigate→defaultCache; server-action POSTs never match (submit path untouched). | ✅ shipped (PRs #113 queue+nav, #114 offline-page `force-dynamic` build hotfix, #115 untrack next-pwa sw/workbox artifacts — all merged 2026-07-06). Follow-ups (non-blocking): `newLocalId` per-submit double-tap → 2 orders (mitigated by disabled button); logout clears neither Dexie queue nor SW page cache (one-salesman-per-device assumption); offline/CatalogShell strings hardcoded ID. (`proxy.ts` `/pwa/sw.js` auth-gating RESOLVED — PR #117 added `/pwa/sw.js` + `/pwa/swe-worker*` to the allow-list.) |
| **7 (Photos)** | Photo documentation on visits (17-07) — `VisitPhoto` table (`clientId @unique`, `visitId` idx) + `attachVisitPhoto` writer (`clientId`-idempotent, visit-ownership via `findFirst` on id+userId, no open-check → attaches post-checkout) + auth'd `POST /pwa/api/upload/visit-photo` (401→403→503→400; `clientId` + ownership pre-checks BEFORE `uploadToR2`; key `visit-photos/<visitId>/<clientId>.jpg`; MIME→ext) + backoffice read-only gallery (per-visit **"N foto" chip** in a Foto column → **gallery modal** grid → full lightbox on store detail; NOT an inline thumbnail strip). PWA: client-side compression (`@/lib/pwa/photo/compress` canvas 1600px/0.8 → ~200-400 KB), offline `pendingPhotos` Dexie queue (v2) + `flushPendingPhotos` reusing `setupOrderSync` (orders→photos, `clientId=localId` server-dedup), `VisitPhotoCapture` (**fullscreen** getUserMedia viewfinder via `createPortal` → capture → **preview with Ulangi/Simpan (retake or keep)**, caption on the preview step; **stream attached in a post-mount effect, NOT synchronously, or the feed renders black**; `<input capture>` fallback routes through the same preview; soft cap 20, per-visit strip synced∪pending with retry, blob-URL revoke, `router.refresh()` on successful sync). Reuses R2 (`@/lib/r2`). Check-in now lands on the store detail (the visit workspace). | ✅ shipped (PR #118 backend+gallery, #119 PWA capture+offline — merged 2026-07-06; smoke fixes #120 check-in redirect→store-detail, #121 camera stream-attach — merged 2026-07-07; UX polish #123 fullscreen capture + keep/retake preview, #124 backoffice "N foto" chip + gallery modal — merged 2026-07-07). Verified on prod. Follow-ups (non-blocking): soft-cap race (advisory; server has no cap); 401-mid-sync marks pending `failed` (recoverable via retry — consider treating auth as transient); lightbox `DialogTitle` + capture strings hardcoded ID (i18n debt). |
| **9 (Store edit approval)** | Store-data edit-with-approval (17-08 edit half) — NEW web-owned `StoreChangeRequest` table (`Store` immutable-until-approve; `pendingKey String? @unique` = one PENDING per store; FK-free migration, `relationMode=prisma`). Salesman (checked-in, visit-bound) proposes name/address/phone/contactName/lat/lng from PWA; `termsType`/`marginPercent`/`paymentTempo`/`code`/`isActive` locked at 3 chokepoints (PWA zod strip + writer typed input + approve `store.update`). Block+show-in-review (form → old→new diff card on `ALREADY_PENDING`). Backoffice inline `StoreChangeReviewCard` on store detail (approve applies **only changed fields** = proposed≠snapshot, preserves concurrent admin edits; reject needs reason) + amber pending badge on store list (rows now fully clickable). Approve/reject `router.refresh()` + `StoreForm key={store.updatedAt}` remount so the form repaints. RBAC reuse `stores:manage`/`stores:view` (nothing to seed). AdminNotification category `STORE_CHANGE_REQUEST`. | ✅ shipped (PR #126 merged 2026-07-08). Follow-ups (non-blocking): dead `requestedByLabel` prop on PWA card; pending query runs even when not checked-in; empty-reason reject reuses `INVALID_STATE` code (unreachable via UI). |
| **10 (Radius check-in)** | Radius validation on check-in (anti-fraud, 17-01 AC) — **warn-only, never blocks**. `checkIn` computes haversine distance from store pin (reuses `@/lib/geo/haversine`), stamps `StoreVisit.checkinDistanceMeters` + `checkinOutOfRadius`; server-authoritative. Effective radius = `Store.checkinRadiusMeters` (admin-only override, NOT in salesman edit whitelist) `?? SystemSetting checkin.radiusMeters` (default/fallback **100m**; override 0 honored, only null→global). Pure helper `@/lib/pwa/checkin-radius` (`evaluateCheckinRadius`/`resolveEffectiveRadius`/`parseRadiusSetting`). Store w/o lat/lng → not flagged. PWA amber soft banner on visit workspace + backoffice amber "Di luar radius · N m" badge in visit history. Checkout not validated. | ✅ shipped (PR #127 merged 2026-07-09). Prod seeded `checkin.radiusMeters=100` (surgical SQL; absent→code falls back to 100). Follow-ups (non-blocking): global setting lookup runs even when per-store override set; banner distance fallback is defensively dead. |
| **11 (Order summary)** | Last-order / recent-orders summary on store profile (17-08 extras) — read-only `getStoreOrderSummary(storeId)` (up to 5 newest non-REJECTED `FieldSalesOrder`; only store-linked order model — marketplace `SalesOrder` has no `storeId`; both putus+konsi). Backoffice "Order terakhir" card (badges + total + clickable rows → field-sales-order detail; konsi total blanked to "—" until APPROVED). PWA "Pesanan terakhir" section (**konsi total hidden** — nulled at page boundary + component guard). No migration/RBAC. | ✅ shipped (PR #129 merged 2026-07-09). **Piutang half SPLIT OUT — buildable, not built.** An AR model now exists (`Receivable`/`Payment`, AR, Collection & Tax epic slice AB, below) and `listReceivables` already filters by `storeId` and returns aging plus a per-store outstanding total, so nothing blocks adding a piutang section to the store profile — it simply hasn't been built. |
| **8 (Canvassing)** | Canvasser mobile van stock (17-05) — decomposed 8a/8b/8c. **8a (van stock foundation + loading) shipped.** NEW `VanStock` ledger (per-canvasser, separate from `InventoryValue` — no location dimension) + `VanLoad`/`VanLoadLine` (`VANLOAD` doc type) + `VAN_LOAD` `StockAdjustmentSource`. Admin `loadVan` (backoffice) = serializable+retry transfer main→van: guard `available = qtyOnHand − reservedQty` all-or-nothing, decrement main `InventoryValue` + audited NEGATIVE `StockAdjustment`, upsert `VanStock` weighted-avg cost, create load doc. Backoffice module (canvasser list + van detail + `LoadVanForm`) gated `canvassing:manage`. **Van load was ITEM-LEVEL at 8a; now PER-VARIANT (Track B, PR #146, 2026-07-23)** — load form picks per-variant (one item block → its in-stock variants), `VanStock`/van docs keyed per real `variantSku`. Inventory lookup tolerates `variantSku` null-or-empty (variantless rows use `null`). VanStock keyed `variantSku ""` for simple items. Cash-on-spot model (no receivable). **8b (on-spot van sale + nota) shipped** — NEW `VanSale`/`VanSaleLine` (terminal POS, no approval; `VANSALE` doc type; NOT `FieldSalesOrder`) + `recordVanSale` writer (serializable+retry, server-priced via `computeStorePrice` PUTUS, **hard-block on short van stock** unlike putus's warn-oversell, deduct `VanStock` only — NO `StockAdjustment`/main untouched, COGS snapshot = VanStock.avgCost, `SalesHistory` channel OFFLINE, `idempotencyKey` dedup). Buyer = registered store OR ad-hoc (nullable storeId + buyer name/phone); no check-in; sale GPS captured. PWA `/pwa/van` sell screen (van as sellable set, qty capped, cash→change) → thermal nota (`van-sale-nota-html`, 58/80mm, print/share). Backoffice `/backoffice/van-sales` register (list/detail/reprint, `canvassing:manage`). `getVanSaleById(id, {salesmanId?})` — PWA scopes to owner + strips COGS from client payload. **8c (EOD reconcile + van→main return) shipped** — NEW `VanReconcile`/`VanReconcileLine` (`VANRECON` doc type) + `VAN_RETURN` source. Admin-backoffice `recordVanReconcile` (serializable+retry): reads current van (`VanStock.qty`=expected=loaded−sold), count set must bijection the van rows (`COUNT_MISMATCH`), returns each `counted` to main (moving-avg blend via **OR-tolerant null-or-empty lookup** like loadVan — NOT `calculateMovingAverage`'s strict ""-key, which forked a phantom row + was fixed pre-merge — + POSITIVE `StockAdjustment` `VAN_RETURN`), **empties the van** (`VanStock.qty→0`), logs variance=expected−counted; **reason required on any nonzero variance** (`VARIANCE_NEEDS_REASON`); empty van → `EMPTY_VAN`; **NO shrinkage adjustment** (shortfall already left main at load). Reconcile form (rows fixed to van, counted prefilled to expected, live variance badge) + history + read-only detail on the canvasser van-detail page. | ✅ **COMPLETE (8a+8b+8c)** — 8a (PR #131 + #132/#133), 8b (PR #139), 8c (PR #141) merged 2026-07-09/10. `canvassing:manage` seeded on prod. **Sub-8 done → EPIC-17 (Field Sales/SFA) complete.** Deferred follow-ups (non-blocking): offline write-queue for van sales; promos on van sales; void/refund. (Variant-aware field sales ✅ done — Track A PR #144 putus/konsi, Track B PR #146 canvassing.) |

Client-feedback batch (meeting 26/07/2026, tracked on GitHub issue **#206** — not an EPIC). Field-sales features shipped outside the EPIC tables; key surfaces so a future session finds them:

- **Price appeal (#207)** — putus is now **server-priced** (`computeStorePrice` PUTUS at create, `field-sales/writer.ts`); the salesman can only *appeal* (`requestedUnitPrice` + `appealReason` on a line), the owner sets the final price at approve via `finalPrices` in `approveFieldSalesOrderAction`. Konsi unchanged.
- **SPG role (#208)** — new seeded `SPG` RoleDefinition + `User.assignedStoreId` (one fixed store). NEW `SpgSale`/`SpgSaleLine` (`SPGSALE` doc type) — a terminal sale (server-priced PUTUS, `SalesHistory` OFFLINE, idempotencyKey). **Stock movement is gated on the store's `termsType`**: a sale at a KONSI store decrements that store's `StoreStock` row (the goods physically leave the consignment shelf); a sale at a PUTUS store still moves no stock at all (the store already owns those goods outright). Check-in/sale server-scoped to `assignedStoreId` + active-visit-required. `recordSpgSale` writer; `/backoffice/spg-sales` register (`spg_sales:view`). Prod RBAC seed `seed-spg-role-permissions.sql` — run 2026-08-08 (role + 2 permissions + links). No UI sets `User.assignedStoreId` yet, so an SPG user still needs that column set by hand.
- **Pack-ratio (#210)** — global size→qty pack config in `SystemSetting["putus.packRatio"]` (Settings → Rasio Pack). Pure `@elorae/db/pack-ratio` `expandPack`/`parsePackRatio`/`validatePackRatio` (client-safe subpath). Perms `settings_pack_ratio:*` (`seed-pack-ratio-permissions.sql`, run on prod 2026-08-08).
- **Smart request (#211)** — PWA putus **cart builder** (`/pwa/stores/[id]/smart-request`): categories + packs → pure `planSmartRequest` (`lib/field-sales/smart-request/plan.ts`) ranks (never-ordered-first → APPROVED qty) + walks 1-pack-per-article via `expandPack`, drops-and-continues on short/missing, → normal putus order. `createFieldSalesOrder` gained a **server-set `skipMinQty`** (smart-request submit passes true; manual catalog cart still enforces min-qty). `getSmartRequestHistory`, `loadSmartRequestCandidates`, `buildSmartRequestAction`/`submitSmartRequestOrder`. Putus-only (KONSI gated).
- **Putus delivery (PR #229, merged 2026-08-09)** — stock leaves at delivery instead of approval; orders ship in parts; nota gudang + nota tagihan print per delivery (`DeliveriesCard` on the order detail); a backfill migration gives every pre-existing approved putus order one full delivery. New permission `field_sales_orders:deliver` — **run `packages/db/prisma/seed-field-sales-delivery-permissions.sql` on prod post-merge.** See `docs/ARCHITECTURE-NOTES.md`.
- **Faktur pajak tracking (PR #239, merged 2026-08-13)** — one `TaxInvoice` per nota tagihan, worked from `/backoffice/finance/faktur-pajak`; see `docs/ARCHITECTURE-NOTES.md`. **Both post-merge steps are DONE — do not re-run them blindly:**
  1. ✅ DONE 2026-08-13 — ran `packages/db/prisma/seed-tax-invoice-permissions.sql` on prod (`tax_invoices:view` + `tax_invoices:manage` + the ADMIN links) — the deploy pipeline migrates but never seeds.
  2. ✅ NO-OP 2026-08-13 — prod had **0 `FieldSalesDelivery` rows**, so the backfill had nothing to insert and the deploy window could not have stranded anything. The step and its reasoning stay recorded because it becomes real the next time this table is migrated with data in it. Originally: **after the new web image is live**, re-run the backfill `INSERT` from `packages/db/prisma/migrations/20260813120000_add_tax_invoice/migration.sql` once. The deploy workflow applies migrations BEFORE the new image goes up, so any delivery recorded by the OLD image in that window gets no `TaxInvoice` at all: it never appears in the finance queue, and its first-print compare-and-swap matches zero rows so finance is never notified either. **Both surfaces fail closed and silent** — nothing anywhere reports the missing row. The `INSERT` is `NOT EXISTS`-guarded (and the whole migration file is now `IF NOT EXISTS`/re-runnable), so re-running it is safe and idempotent.
- **Konsi "baru" (new product) is an ASSUMPTION, not a client answer.** The konsi client story asks the admin to substitute "barang-barang yang **baru** dan belum pernah kirim" when the salesman's requested SKUs are short. Never-sent is answerable today (`sentItemIds`); **new is not defined anywhere**. Working assumption pending client confirmation: new = `Item.createdAt` within N days, N held in `SystemSetting["konsi.newItemDays"]` (default **90** — roughly one apparel season — with an absent row falling back in code, same shape as `checkin.radiusMeters`). Open questions recorded on card #22: whether "baru" means a new *product* or a new *production run*, whether 90 days is the right window, and whether a new product should be suggested even to a store that has had it before. Build the never-sent half first; it is unaffected by the answer.
- **Store price-rule (#206) — client unblocked it in the issue comments (Option A: `Store.priceDiscountPercent`, standing base, PUTUS only, store detail form); shipped on branch `feat/store-price-discount`, PR #261, merged 2026-08-26.** Stacking order: store base discount → Promo campaigns → price appeal at approval. `null`/`0` = no adjustment; cents-rounding now lives inside `computeStorePrice` for both PUTUS and the KONSI gross-up. See `docs/ARCHITECTURE-NOTES.md`. Reminder: the **Promo engine (#111)** already covers per-store/per-item PERCENT/FIXED/TIERED discounts and was deliberately NOT reused for this (Option E rejected) — the gap this closes is standing base-price granularity, not campaign discounting.

EPIC-07 (Stock Opname & Reconciliation) decomposition:

| Sub | Scope | Status |
|----|-------|--------|
| **1** | Schema, RBAC, SystemSetting defaults, OPN doc type | ✅ shipped (2026-06-28) |
| **2** | Opname backend (snapshot, count, submit, approve w/ drift, fabric rolls) | ✅ shipped (2026-06-28) |
| **3** | Opname UI (list, create, scope-branched count, drift modal) | ✅ shipped (2026-06-28) |
| **4** | Reconciliation engine + API inventory snapshot | ✅ shipped (2026-06-28) |
| **5** | Cron route + jobs.ts 6h schedule + reconciliation admin UI + docs | ✅ shipped (2026-06-28) |

Launch posture: reconciliation `FLAG_ONLY` + threshold 0; auto-correct directions implemented but dormant until config flip.

EPIC-11 (Marketplace Settlements) + EPIC-15 (Marketplace Adapter) decomposition — **both shipped incrementally during the settlement work and were never given a table here**; reconstructed 2026-08-05 by auditing the code against the two cards. Board issues stay OPEN pending the client's own verification of the match-rate and profit ACs:

| Story | Scope | Status |
|----|-------|--------|
| **11-01** | Settlement upload + parse — `POST /api/finance/settlements/upload` → `parseSettlement()`; Shopee reads all four sheets (Summary/Income/Adjustment/Seller Fee), 43 Income columns; parse failures return 422 with `{sheet, row, message}[]` | ✅ shipped. Error *display* was broken until `fix/settlement-parse-error-display` (the client typed the payload as `string[]` and rendered objects into JSX → React "Objects are not valid as a React child" crash on any malformed upload) |
| **11-02** | Match orders — `matchSettlement` + `salesorderNoForSettlement` (`match-key.ts`, prefix-aware: Shopee `salesorder_no`, TikTok/Tokopedia `ref_no` OR `salesorder_no`); unmatched list + per-line resync | ✅ shipped (PRs #165/#166) |
| **11-03** | Profit per order — `cogsSnapshot` + `profit = netIncome − cogs` per `SettlementLine`; reconciling KPI trio (`matchedNetIncome − totalCogs = totalProfit`) + `matchRatePct`, `unmatchedCount`, `profitPendingCount` | ✅ shipped |
| **11-04** | Reconcile + journal — `checksumOk` gate returns `CHECKSUM_BLOCKED` before any posting; `postSettlementJournal` then posts DR Bank + per-fee-category DR + CR AR | ✅ shipped (PR #157; fee split + sign fix in the financial-reports PR) |
| **15-01** | Shopee adapter — `shopee-settlement-parser.ts` → `ParsedSettlement` | ✅ shipped |
| **15-02** | TikTok adapter — `tiktok-settlement-parser.ts`, same `ParsedSettlement` shape; zeroes the four Shopee fee columns (TikTok itemizes nothing per line), derives period dates by scanning Date cells, normalizes `Total Biaya` with `Math.abs()` | ✅ shipped (PR #168, validated on a real 18-line TikTok income export) |
| **15-03** | Marketplace selection on upload — `SUPPORTED_MARKETPLACES` dropdown (SHOPEE/TIKTOK) → `parseSettlement()` switch; unknown value → 400, wrong-format file → 422 parse errors (never silent corruption) | ✅ shipped |

Caveat on 15's "identical normalized schema" AC: the *shape* is identical, but fee itemization is not — Shopee fills four fee columns, TikTok fills none, so a TikTok Laba Rugi shows all fees under `MARKETPLACE_FEE_OTHER`. Downstream matching/profit/reconciliation are genuinely marketplace-blind. `TOKOPEDIA` resolves in `match-key.ts` but has **no parser** and is absent from the dropdown — out of scope for EPIC-15 (Shopee + TikTok only).

EPIC-13 (Journal Engine) decomposition:

| Story | Scope | Status |
|----|-------|--------|
| **Engine** | Balance-enforced `postJournal` (cents-rounded, idempotent by `(sourceType, sourceId)`) + posting-role→account mapping (`JournalAccountMapping`, `resolveAccount`, `POSTING_ROLES`) + manual journal entry (13-07) + ledger list/detail view (13-08) | ✅ shipped (PR #155 core + PR #156 mapping, merged 2026-07-23) |
| **13-04** | Auto-journal: settlement — DR Bank + DR Fee, CR AR (Piutang); checksum-gated | ✅ shipped (PR #157 merged 2026-07-24) |
| **13-06** | Auto-journal: opname adjustment — surplus DR Persediaan / CR Selisih (shrinkage reversed); net from `StockMovement` (FG+accessories+fabric); best-effort post-approve + `JOURNAL_PENDING` flag + retriable "Post journal" button (gated on non-zero delta). Introduced the generic `generateAutoJournal(client, sourceType, sourceId, lines, meta)` seam all future auto-journals reuse. | ✅ shipped (PR #159 merged 2026-07-25) |
| **13-01** | Auto-journal: GRN — DR Persediaan, CR Hutang; best-effort on create + reverse (DR Hutang / CR Persediaan) on owner-decline of an over-receive GRN; value = `totalAmount`, dated `grnDate` | ✅ shipped (PR #160 merged 2026-07-26) |
| **13-02** | Auto-journal: FG receipt — DR Persediaan FG (`INVENTORY_FG`) / CR Persediaan (`INVENTORY`, raw), value = `FGReceipt.totalCostValue`, dated `receivedAt`; create-only, best-effort on `receiveFG`; adds the `INVENTORY_FG` role (+ mapping i18n). Credit = raw inventory (transfer), NOT WIP/HPP — material issue isn't journaled | ✅ shipped (PR #161 merged 2026-07-26) |
| **13-03** | Auto-journal: sales (marketplace) — revenue (DR AR / CR SALES_REVENUE @ `grandTotal`) + COGS (DR COGS / CR INVENTORY @ Σ `SalesOrderItem.cogs`); posted at ship by a **5-min web cron sweep** (`lib/finance/sales/sweep.ts` + `jobs.ts`) over un-journaled shipped orders (raw `NOT EXISTS`; eligible = `status IN (SHIPPED,COMPLETED) OR fulfillmentStatus=SHIPPED`), idempotent by source, read-only badges on the order detail. Cross-service (ship fires from api webhook + web button via `consumeOrder`; sweep is the web-side bridge) | ✅ shipped (PR #163 merged 2026-07-26) |
| **13-05** | Auto-journal: retur — reverse of 13-03 on a finalized return (DR SALES_REVENUE / CR AR @ Σ accepted subtotal + DR INVENTORY / CR COGS @ Σ restock cost); best-effort at `submitReturnDecision`, aggregating ACCEPTED items; web-only (no cron), retriable. Also fixed a pre-existing Decimal server→client leak in `getSalesReturnById` | ✅ shipped (PR #164 merged 2026-07-26) |

**EPIC-13 (Journal Engine) COMPLETE** — all six auto-journals shipped (13-01 GRN, 13-02 FG receipt, 13-03 sales/COGS, 13-04 settlement, 13-05 retur, 13-06 opname adjustment) on the shared `generateAutoJournal` seam + role→account mapping. Any future auto-journal reuses the seam — add: role lines + source hook + spec + test. **Adding a NEW `POSTING_ROLES` entry also requires its `financeAccountMapping.role.<ROLE>` + `.desc.<ROLE>` i18n in BOTH locales** (else the account-mapping row renders a raw key — the 13-02 final-review catch) **and an entry in `POSTING_ROLE_ACCOUNT_TYPES`** (`apps/web/lib/finance/journals/role-account-types.ts`) — the `Record<PostingRole, …>` type would normally catch a missing entry, but `apps/web` type-check never runs here and vitest strips types at runtime, so the completeness test plus this doc line are the only real nets. Requires the roles mapped in the CoA; unmapped → flagged (`JOURNAL_PENDING`) + retriable, never blocks the source op.

Already done before sub-1: 01-01 (token + cron + alert), 01-04 (API call audit log + 429 + admin dashboard), catalog ingest (`POST /jubelio/catalog/sync`), category sync (2026-06-05).

**Maintenance rule:** When a sub-project, EPIC, or independently-shipped story merges to master, update the relevant table row in this file (`docs/EPIC-STATUS.md`) in the same session (status → ✅, append PR # + merge date). Stale decomposition tables caused at least one false-start ("sub-2 next" when sub-2 had shipped months earlier). Treat the table as part of the merge checklist, not an afterthought. Same for the **Follow-ups / tech debt** checklist in `docs/FOLLOWUPS.md` — on merge, tick any shipped items (append the PR #) and move any new follow-ups there (never leave them only in PR bodies). **Also tick the matching story on the GitHub board** (the epic issue, `EPIC-NN` → #`NN+4`) — that's the canonical cross-EPIC status tracker now that `reference/todo/` is retired.

EPIC-14 (Financial Reports) decomposition:

| Story | Scope | Status |
|----|-------|--------|
| **14-01** | Trial Balance — period-range debit/credit/saldo over postable leaves; balance check framed as a corruption tripwire (`postJournal` already enforces per-journal balance) | ✅ shipped (PR #215 merged 2026-08-05). Prod permission seeded 2026-08-08. |
| **14-02** | Laba Rugi — Pendapatan − HPP = Laba Kotor − Beban = Laba Bersih; sections by `AccountType` (never account code); GL-coverage banner naming the un-journaled sources | ✅ shipped (PR #215 merged 2026-08-05). Prod permission seeded 2026-08-08. |
| **14-03** | Neraca — as-of, cumulative; balances via an injected "Laba (Rugi) Belum Ditutup" line standing in for the missing fiscal close; opening-balance warning when no journal predates the ledger | ✅ shipped (PR #215 merged 2026-08-05). Prod permission seeded 2026-08-08. |
| **14-04** | Export — Excel via `xlsx` server actions returning `{base64, filename}`; PDF via `/print/finance/*` + `print.css` browser print | ✅ shipped (PR #215 merged 2026-08-05). Prod permission seeded 2026-08-08. |
| **Fee split** | Settlement journal posts one line per fee category (`MARKETPLACE_FEE_ADMIN`/`_SERVICE`/`_COMMISSION`/`_PROCESSING`) plus a `_OTHER` residual computed from `totalPengeluaran` so the journal still balances; unmapped category roles fall back to the legacy `MARKETPLACE_FEE` account. TikTok zeroes all four columns → all its fees land in `_OTHER`. Zero schema migration (`role` is a String). | ✅ shipped (PR #215 merged 2026-08-05) |

EPIC-16 (Cash Flow) decomposition:

| Story | Scope | Status |
|----|-------|--------|
| **16-01** | Cash flow generation — indirect method from journal data; net change derived from every non-cash account's period delta, so `ΔKas = LabaBersih + ΔLiabilitas + ΔEkuitas − ΔAsetNonKas` holds identically and the reconciliation is a corruption tripwire, not a real check. Section comes from a nullable `ChartAccount.cashFlowSection` override falling back to a read-time deriver (`BANK`/`CASH` ⇒ KAS, `AR`/`AP`/`INVENTORY*` ⇒ OPERASIONAL, `EKUITAS` ⇒ PENDANAAN, everything else unclassified). Unclassified accounts stay inside the total and render in their own bucket, so classification affects presentation only — **except for `KAS`, which defines what "cash" means**, and is therefore restricted to `ASET` leaves at three chokepoints (action guard, disabled picker option, leaf-only section map). | ✅ shipped (PR #235 merged 2026-08-11) |
| **16-02** | Period comparison — immediately-preceding equal-length window, per-line delta over the union of both periods' accounts. Hidden when the range is since-inception. | ✅ shipped (PR #235 merged 2026-08-11) |
| **16-03** | Export — Excel via `exportCashFlowExcel`, PDF via `/print/finance/cash-flow`. Print is single-period by design; three amount columns do not fit the portrait sheet. All three surfaces refuse to render when no account is classified `KAS`, rather than emitting zeros. | ✅ shipped (PR #235 merged 2026-08-11) |
| **Klasifikasi** | Finance → Klasifikasi Arus Kas, reusing `journals:view`/`journals:manage` — **no new permission rows, nothing to seed on prod**. The section map is leaf-only but deliberately RETAINS inactive leaves: `getAccountBalances` keeps an inactive account that still has movement, so dropping it from the map would lose its balance from `kasAwal` while its rows still reached the engine — understating closing cash with no visible signal. | ✅ shipped (PR #235 merged 2026-08-11) |

**Dormant until the GL cutover is armed** (see the GL blocker in `docs/FOLLOWUPS.md` § Finance — Journal & CoA): with no journals posting, the statement renders honest and empty, and the reconciliation criterion cannot be verified against real numbers.

Retur Management decomposition:

| Story | Scope | Status |
|----|-------|--------|
| **Retur request from field** | Salesman raises a store retur from the PWA: lines (item + variant + qty + reason), a required photo of the store's paper nota retur, and how the goods travel (`SELF_CARRY`, or `EXPEDITION` with carrier name AND resi, both enforced server-side). Submitted online-only through `POST /pwa/api/retur`. Own `DocType.FIELDRET` (prefix `FRET/`) — deliberately NOT `RET`, the vendor-return sequence, so the two registers don't interleave on one counter. | ✅ shipped (PR #245 merged 2026-08-24) |
| **Warehouse independent receiving** | The office counts what physically arrived against the claim, splitting each line into sellable and rejected. Count inputs start EMPTY — not pre-filled from the salesman's claim — while the claimed qty shows in an adjacent read-only column by design, so the admin transcribes from the store's paper. | ✅ shipped (PR #246 merged 2026-08-24) |
| **Mismatch flagging** | Per-line status + mismatch report (claimed vs received vs difference), plus an admin `AdminNotification`/bell fan-out (`FIELD_RETURN_MISMATCH` category). | ✅ shipped — status/report (PR #246 merged 2026-08-24) + admin notification (PR #250 merged 2026-08-25). **The salesman half is NOT built and is blocked** — the PWA has no notification surface at all (no bell, no list, no FCM registration), and `fanOutAdminNotification` resolves recipients by permission rather than by user, so "tell the salesman who raised this" has no existing mechanism. |
| **Shortage liability resolution** | Append-only `FieldReturnResolution` rows; `SALESMAN_BEARS` / `INVESTIGATE` / `WRITE_OFF` / `ACCEPT_SURPLUS`; latest resolution wins under a total ordering; `INVESTIGATE` deliberately settles nothing; direction is enforced in the writer, so only `SALESMAN_BEARS`/`WRITE_OFF` settle a shortage and only `ACCEPT_SURPLUS` settles an over-line. | ✅ shipped (PR #246 merged 2026-08-24) |
| **Retur approval & stock update** | Approval restores `sellableQty` to `InventoryValue` with a POSITIVE `StockAdjustment` sourced `FIELD_RETURN` and routes `rejectedQty` to `RejectedGoodsLedger`. Value (credited qty via an explicit per-resolution-type mapping that must never be collapsed to `Math.max`) is frozen in the same serializable transaction. | ✅ shipped across PR #246 (stock, merged 2026-08-24) + PR #248 (value, merged 2026-08-25) |
| **Retur value link to settlement** | Would auto-populate approved retur value as a settlement deduction and stamp it `APPLIED` to prevent double use. | **NOT built and BLOCKED** — an AR model now exists (`Receivable`/`Payment`, AR, Collection & Tax epic slice AB, below), but nothing lets a retur's value offset a receivable; that is slice G (Retur-offset payment) of the same epic, deliberately cut from slice AB because the retur value → settlement link was itself blocked. The `APPLIED` marker is still not built — nothing can apply a retur until slice G ships, and a column nothing writes is worse than no column. |

Retur Management is complete except for two blocked stories: the salesman half of mismatch flagging (blocked on the PWA having no notification surface at all) and the retur value link to settlement (blocked on the retur-offset slice of the AR, Collection & Tax epic, not on a missing AR model — see the epic decomposition further down this file).

Inventory Extensions decomposition:

| Story | Scope | Status |
|-------|-------|--------|
| **Virtual warehouse (consignment store)** | Approving a konsi order now physically moves stock instead of only reserving it forever. Inside the approve transaction: main `qtyOnHand` AND `reservedQty` decrement together, a NEGATIVE `StockAdjustment` sourced `KONSI_TRANSFER` is written, a per-store `StoreStock` ledger is upserted at a weighted-average blended cost, the line's `StockReservation` flips to `CONSUMED`, and a `KonsiTransfer` document is issued with a `KTRF/` number. The surat keluar print moved off the order and onto that transfer. **A virtual warehouse is the `Store` row itself** (`termsType = KONSI`) — there is no separate warehouse entity, mirroring how `VanStock` keys on `userId` with no `Van` entity. Revisit trigger: introduce a real `Warehouse` entity only if virtual→virtual transfer proves a genuine client need. | ✅ shipped (PR #253, merged 2026-08-25) |
| **Stock transfer (inter-warehouse)** | main→virtual ships here as the approve path above. virtual→main now ships two ways: a salesman-raised field retur (unchanged), and an admin-initiated store return (PR #258, merged 2026-08-25) — an admin picks a store, sees its `StoreStock` balance, and raises a `FieldReturn` with `origin: ADMIN` against it, so it still runs through receive/mismatch/approve rather than moving in one shot. virtual→virtual is still NOT built. | 🟡 PARTIAL |
| **Stock movement tracking** | The store detail card derives a per-store movement list from the documents that actually moved store stock (`KonsiTransferLine`, approved `FieldReturnLine`), not from a dedicated ledger table. There is no movement ledger table, and no cross-warehouse running balance. | 🟡 PARTIAL |
| **Canvasser mobile stock** | Per-canvasser `VanStock` ledger + load/sale/reconcile documents. Already shipped previously — see Field Sales/SFA sub-8 (Canvassing) above. This branch did not build it; listed here only so the Inventory Extensions table is a complete picture of "who holds stock outside main". | ✅ shipped previously (not this branch) |

Inventory Extensions remains PARTIAL: virtual→main now ships (via a salesman-raised or an admin-initiated field retur, PR #258), but virtual→virtual transfer and a real stock-movement ledger table with a cross-warehouse running balance are both still open.

Consignment Lifecycle decomposition (tracked on GitHub issue #27; all four planned slices merged 2026-08-26):

| Story | Scope | Status |
|-------|-------|--------|
| **Store consignment configuration** | `Store.marginPercent` + `Store.paymentTempo` exist and back the gross-up (`computeStorePrice`'s KONSI branch, `@elorae/db/pricing`: `sellingPrice / (1 - marginPercent/100)`). **The default per-store assortment now exists** (slice 4, PR #259, merged 2026-08-26): `StoreAssortmentLine` (per-variant, nullable `targetQty`) is CRUD-managed from the store detail card and surfaced on three read-only surfaces — a gap section on the store stock card, gap rows folded into the existing konsi-suggestions panel on an order under approval, and assortment SKUs prefilled into stocktake count sheets. It is reference data with no `DocType`/status/writer lifecycle and gates nothing — no admin action can push stock against a gap directly, so a gap is only ever acted on when a salesman happens to raise a konsi order at that store; see `docs/ARCHITECTURE-NOTES.md`. Templates/bulk-copy between stores and any staleness reminder were deliberately deferred — see `docs/FOLLOWUPS.md`. | ✅ shipped (feat/store-assortment, PR #259) |
| **Consignment stock transfer** | Approving a konsi order moves stock main → `StoreStock` and issues a `KonsiTransfer` (`KTRF/` prefix); the surat keluar print moved onto that transfer. Full writeup at Inventory Extensions § Virtual warehouse (consignment store) above. | ✅ shipped (PR #253, merged 2026-08-25) |
| **Auto-suggest never-sent SKUs** | `listKonsiSuggestions` lists goods never sent to a store (item-level exclusion via `sentItemIds`) and lets the approving admin stage additions onto the same konsi transfer, priced by the same gross-up. See `docs/ARCHITECTURE-NOTES.md`. | ✅ shipped (PR #242, merged 2026-08-13) |
| **SPG sell-through reporting** | `SpgSale`/`SpgSaleLine` record a terminal POS sale (server-priced retail-to-customer, `SalesHistory` written at sale time — see the AR note below). As of this slice, a sale at a KONSI store also decrements that store's `StoreStock` row inside the same transaction (`apps/web/lib/spg/sale-writer.ts`). The store stocktake (PR #257, below) now gives an assigned SPG a PWA path to submit something to the admin for review — but that something is a physical shelf count, not a sales report. **The periodic sell-through report itself still does not exist** — nothing lets an SPG submit a day's or month's SKU+qty-sold as a batch for admin review; today's `SpgSale` remains a per-transaction POS record, not a reporting document. | 🟡 PARTIAL |
| **Sell-through verification** | `qty sent − qty sold − qty returned = qty remaining on counter`, with discrepancy flagging and approve-if-balanced / hold-for-investigation. **What shipped instead (the store stocktake, PR #257, merged 2026-08-25):** an admin (no SPG required) counts a store's shelf and approves, overwriting `StoreStock.qty` with the counted figure; an assigned SPG may submit the count from the PWA for the admin to verify. `varianceQty = countedQty − expectedQty`, where `expectedQty` is the store's live ledger `StoreStock.qty` at the moment the count opens — itself already the running net of every transfer/sale/retur — so the mechanism reconciles the same quantity the original formula describes, just by diffing against the ledger rather than re-deriving sent/sold/returned separately. **No hold-for-investigation**: a nonzero variance needs a reason and a shortfall needs a cause, but approval then always applies the count — nothing pauses a discrepant line for review. A blank count means "not counted" and writes nothing for that row; `isFullCount` records whether every line was counted. This is now the only thing that measures or corrects the historical `StoreStock` overstatement below — but only at a store someone actually counts. Nothing schedules, prompts or reminds a stocktake. | 🟡 PARTIAL |
| **Sell-through invoicing (AR at sell-through)** | Nota tagihan generated from a verified sell-through, with piutang created only at that point, not at transfer. **Blocked** — an AR model now exists (`Receivable`/`Payment`, AR, Collection & Tax epic slice AB, below), but nothing creates a `Receivable` at sell-through: today the only source document is a putus `FieldSalesDelivery`. Konsi sell-through has no receivable-capture path yet, and no client-facing "verified sell-through" step exists to trigger one from. What's missing is the sell-through capture, not the AR model itself (same remaining shape as putus/konsi field-sales revenue; see `docs/FOLLOWUPS.md`). | ❌ blocked |
| **Consignment stock return** | A salesman-raised field retur at a KONSI store already decrements `StoreStock` on approval (`apps/web/lib/field-sales/retur/approve-writer.ts`) — see `docs/ARCHITECTURE-NOTES.md`. **The admin-initiated flow shipped (slice 3, PR #258, merged 2026-08-25):** an admin picks a store, sees its `StoreStock` balance, and selects SKUs/qty to push back — but it is **not** a standalone return-transfer document moving stock in one shot outside any retur claim, as originally scoped. It shipped as `FieldReturn` with a new `origin: ADMIN`, reusing the existing receive/mismatch/resolution machinery, because the warehouse still has to physically count what arrives. `StoreStock` decrements in two steps as a result — `receivedQty` at the warehouse count, the delta at approval — not in one atomic transfer like `KonsiTransfer`; see `docs/ARCHITECTURE-NOTES.md` for the split and its in-transit-window cost. | ✅ shipped (PR #258, merged 2026-08-25) |

Consignment Lifecycle remains PARTIAL after all four slices. The store stocktake (slice 2, PR #257) shipped and is the mechanism that finally makes the historical `StoreStock` overstatement correctable — but only where someone performs a count, since nothing schedules one, and it reconciles by direct count against the ledger rather than by re-deriving the sent/sold/returned formula the sell-through verification story originally specified. The admin-initiated store→main return (slice 3, PR #258) also shipped, via the field-retur mechanism rather than a dedicated transfer document — see the Consignment stock return row above. **Slice 4, the default per-store assortment, shipped** (PR #259, merged 2026-08-26), which closes the epic's *buildable* scope but not the epic itself: the assortment gates nothing and has no admin-initiated push transfer of its own, so a gap is only ever acted on when a salesman happens to raise a konsi order at that store — the single biggest limit on its usefulness (see `docs/FOLLOWUPS.md`). Sell-through reporting and sell-through verification as originally specified remain absent (the store stocktake substitutes for verification but does not implement the periodic sent/sold/returned report), and sell-through invoicing/AR stays blocked — no longer on a missing AR model (see the AR, Collection & Tax epic below), but on there being no sell-through-triggered receivable-capture path yet. The epic is not finished.

AR, Collection & Tax decomposition (GitHub issue #25). Six slices; the epic merges the sales-invoicing gap (no offline order revenue reached the GL) with a genuine piutang/collection workflow:

| Slice | Scope | Status |
|-------|-------|--------|
| **AB** | AR ledger + payment recording — `Receivable` (one per putus `FieldSalesDelivery`, aging in six buckets), `Payment` + `PaymentAllocation` (multi-receivable allocation, cash/transfer, proof upload), void with a reversing entry, four auto-journals (delivery revenue/COGS, payment receipt/void) via `postArJournalSafely`, backfill (`Receivable` only, no journals) for every pre-existing delivery | ✅ shipped (PR #263 merged 2026-08-26) |
| **C** | Credit limit — `Store.creditLimit` (nullable, `null`=unlimited); order-create stays permissive (advisory `creditHoldAtCreate` flag only), the real gate is a live exposure recompute in `approveFieldSalesOrder`, refused unless the approver supplies an override reason (new `field_sales_orders:credit_override` permission); konsi exempt by construction | ✅ shipped (PR #269 merged 2026-08-27) |
| **D** | Collection assignment — `Receivable.collectorId` is a workflow column, not a separate assignment table/entity; PWA submission queue (a submission moves no money, it's a `PENDING` claim); backoffice verification queue (verify/reject); three real states `PENDING`→`VERIFIED`/`REJECTED` (no "Collected" intermediate — money moves only at verify, via the existing `recordPayment` writer) | ✅ shipped (PR #273 merged 2026-08-27) |
| **E** | Overdue notification — cron, configurable thresholds, overdue export | Not started |
| **F** | e-Faktur completion — NPWP/PPN fields on the shipped `TaxInvoice`, "Sent to Store"; most of the epic's e-Faktur story already shipped as faktur pajak tracking (PR #239) | Not started |
| **G** | Retur-offset payment — pay a receivable with an approved retur's value; deliberately cut from slice AB because the retur value → settlement link was itself blocked | Not started |

Slices E and G still need only AB; F is independent of everything. C and D both shipped on top of AB (see `docs/superpowers/specs/2026-08-27-credit-limit-enforcement-design.md` and `docs/superpowers/specs/2026-08-27-collector-assignment-design.md`). See `docs/superpowers/specs/2026-08-26-ar-ledger-payments-design.md` for slice AB's design and its deliberate limitations (collector assignment — now shipped as slice D — plus overdue alerts, giro, retur offset, write-off, unapplied credit, konsi sell-through AR, or historical GL backfill — see `docs/FOLLOWUPS.md` and `docs/ARCHITECTURE-NOTES.md`).
